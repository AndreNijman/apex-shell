# APEX Shell UI/UX redesign — implementation status

Working tracker for the coordinated implementation of
`APEX-Shell-Master-UIUX-Roadmap-v3.md` (engineering order, lifecycle, performance,
QA) and `APEX-Shell-Fluid-Visual-Design-Roadmap-v1.md` (what it looks and moves
like). It is not a third roadmap: it records where each phase is, what it touched,
how it was verified, and every place the implementation deliberately departs from
the text.

Branch: `feat/uiux-redesign` (from `origin/main` @ `17eec38`). Worktree:
`/var/tmp/apex-work/wt-uiux-shell`.

Statuses: NOT STARTED · IN PROGRESS · BLOCKED · IMPLEMENTED (built, tests pass) ·
VERIFIED (built, tests pass, visually reviewed at 1x and slow motion, scrutinised).

## Decisions that shape everything below

| # | Decision | Evidence / reason |
|---|---|---|
| D1 | Fluid geometry renders with `QtQuick.Shapes` + CurveRenderer from a path built each frame out of named parameters; no MSAA layer. | Measured: Shape +0.17 ms/frame vs Canvas +1.65 ms/frame (`tests/visual/bench-renderer.sh`, BASELINE §3). |
| D2 | The geometry itself is a plain-JS module (family, progress, params → path + bounds + content clip), unit-tested with node like `agentstate.js`. | Continuity, no self-intersection and monotone bounds become assertions a CI runner without a compositor can check. |
| D3 | Bar and popup stay separate layer surfaces. One lifecycle object per surface per screen owns `progress`. **Revised in Phase 9:** no moving edge is shared between two windows — the surface draws every part of the notch that moves (band, shoulder, a cover over the bar's own corner) and the bar draws only what never moves. At progress 0 the surface's shape over the notch is the bar's own, so map/unmap is invisible. | Measured: with the bar widening in step from the same integer function, the two layer surfaces presented their frames independently and the notch sat one frame off the body (35-42 px ledges on close/cold open). With the band in the popup window, every frame of every sequence measured seam-exact. The Dashboard already covered its notch the same way. |
| D4 | `SeamlessBarShape` moved off Canvas in Phase 9 (earlier than planned) onto `geometry.js barSilhouette` + CurveRenderer, same corners and rounding. | Measured: the Canvas (8x MSAA layer) repainted a frame or more behind its inputs — up to 77 px of notch lag mid-pour — at 2.28 vs 0.80 ms/frame. Only one Canvas changed, per §27.2's intent. |
| D5 | Motion is its own singleton (`Motion`, registered once in `src/qmldir`), not tokens inside `ThemeSet`. | `tests/lib/theme-stub.sh` regex-parses ThemeSet property declarations; motion is not a per-output quantity. |
| D6 | Roadmaps copied into `roadmaps/` in apex-shell. | `ROADMAP/` above the repos is not under version control. |

## Phase map

| Phase | Status | Files / components | Depends on | Tests / artifacts | Risks |
|---|---|---|---|---|---|
| 0 Baseline | IMPLEMENTED | `roadmaps/baseline/BASELINE.md`, `sheets/`, `tests/visual/{capture-surfaces.sh,contact-sheet.py,bench-renderer.*}`, `tests/lib/headless.sh` (`HEADLESS_WLR_RENDERER`) | — | capture + bench reproducible | OSD/notifications/lock/mixed-refresh not capturable headless without a source; recorded as gaps |
| 1 Motion architecture | IMPLEMENTED | `src/theme/{Motion.qml,motion.js,anim/Motion{Color,Fade,Move}.qml}`, `SettingsService` (motionSpeed/motionScale, animDuration migrated by ratio), `LayoutPage`, `tests/check-reduce-motion.sh` (the motion lint), `tests/motion-test.js` | 0 | motion-test 107/0; lint 22/0 incl. self-tests; 403 literals → 8 (AppLauncher) + 2 allowlisted countdowns; 0 ungated loops | legacy `Theme.animDuration` (40 sites) and named easings (35) remain in the surfaces not yet on SurfaceLifecycle — ratchets |
| 2 Visual tokens | IMPLEMENTED | `ThemeSet`: radius roles XS–Full, `notchShoulder`/`notchBottom`, spacing scale, control heights (compact/standard/comfortable/prominent, rows, tile, hit sizes), type-role sizes; `roles.js` + `Colors`/`Theme`: surface roles (base/raised/overlay/high/selected, hover/pressed layers), accentContainer/onAccentContainer/accentText/onAccent, outlines + hairline, text primary/secondary/tertiary, icon default/active; font families (ui = fontconfig sans → Noto Sans, mono, icon = JetBrainsMono Nerd Font) | 0 | check-scale-tokens 50/0; `color-roles-test.js` 16/0 over the twelve matugen 4.2.0 palettes (fixture with provenance) | tokens are defined, not yet adopted — adoption is Phases 3, 15, 16 (and the 211 translucent whites, 18) |
| 3 Interaction primitives | IMPLEMENTED (core) | `src/components/controls/`: `ApexPressable` (press dip to pressScale on pressIn/pressOut, Space/Return replay it and activate, a state layer `tint(base)` / `stateLayer()` over any surface, focus ring only for keyboard-given focus — tracked, since Qt Quick 6.10 has no Item.focusReason — separate `hitMargin`, disabled at .38), `ApexFocusRing` (2 px accent at radius + 2, 2 px gap), `ApexIconButton` (bar mode: no fill, 24 px target). Migrated: CfgButton, CfgTile, ProfileButton (APIs unchanged; roles instead of translucent whites) | 1, 2 | `run-controls-primitives-test.sh` 10/0; a11y 26/0, settings-controls 16/0, rtl 37/0, slider-wheel 11/0 (staged via the new `stage_roles`); white-foreground ratchet 211 → 204 | IconBtn (bar) moves in Phase 15; CfgSwitch/CfgSlider/CfgSegmented and the ApexToggle/ApexSlider/ApexSelectionPill wrappers are Phase 16 |
| 4 Parametric shape engine | IMPLEMENTED | `src/shapes/fluid/{geometry.js,FluidShape.qml}`, `tests/fluid-geometry-test.js`, `tests/visual/fluid-harness.{sh,qml}` | 1 | geometry suite 129/0; harness sheets reviewed, joins zoomed | GPU cost of CurveRenderer unmeasured |
| 5 Shape families | IN PROGRESS | CENTER_BLOOM, RIGHT_POUR (+ `rightPourWidth`; now draws the notch band, shoulder and corner cover), LEFT_SPILL, EDGE_SPILL (right); `barNotch`, `barSilhouette` | 4 | geometry suite 147/0: per-family sweeps, character checks, pour-at-0 equals the bar notch pixel for pixel, corner cover, cover clear of the icons (mutants caught) | CAPSULE, PIVOT_POP, QUIET_SHEET, LENS_REVEAL, STACK_REFLOW are motion-only families, built with their surfaces |
| 6 Surface lifecycle | IN PROGRESS | `SurfaceLifecycle.qml` (linear progress on open, fastDecel on close, content delay 40/in 130/out 70); under Reduce Motion a close holds the finished shape while it fades (brief B.10); a close+reopen in one tick is a no-op. Dashboard and the right panel migrated | 1 | lifecycle suite 15/0 | remaining popups migrate with their redesign; the CI applyOpenState step now asserts the right panel's clock waits for the panel |
| 7 Connected navigation | IMPLEMENTED | `TabSwitcher.qml`: one pill per switcher travels to the chosen tab (selection token, emphasizedDecel; armed after first placement); tabs draw hover only. `LazyPage.qml`: directional enter/exit (pageTravel, page token), interruption-safe. Dashboard derives direction from tab order via `shownPage` | 1, 3 | nav-geometry 4398/16 (pre-existing), wheel 16/0, settings-controls 16/0, a11y 26/0, rtl 37/0, popup smoke clean; `capture-surfaces.sh … tabs` sheets fwd/back | NavPane done in Phase 13 |
| 8 Dashboard v2 | IMPLEMENTED (motion) | `Dashboard.qml` on CENTER_BLOOM + SurfaceLifecycle, content at final layout under the bloom clip; `TopBar.cWidth` no longer widens for it; `SeamlessBarShape` on the notch tokens, whole-pixel positions | 4–7 | captures cold/warm at 2.5x (`tests/visual/capture-surfaces.sh`), shoulder zoom; popup/nexus smoke, scaling 88/0 | page redesign is Phase 17 |
| 9 Right quick surfaces | IMPLEMENTED | 9a: `RightPanel.qml` — Network, the notification centre, audio and the toast as panes of ONE RIGHT_POUR surface per screen (`NetworkPane`, `NotificationsPane`, `AudioControl`, `NotificationToast` as Items); clock `TopBar.rightLife`, starting only once the panel is built; the bar's notch never moves (the panel draws the band); pane switch keeps the body open and retargets W/D over `page`, panes cross-fade; `OpenPill` on the owning glyph (wifi/bt/vpn/hotspot, speaker, bell), no ▾; network tabs arrive directionally. 9b: Audio moved from the right strip to a pane under its own notch trigger (`AudioPopup` removed); `QuickControl` on EDGE_SPILL as a PanelWindow spanning the right strip, built by the strip hover (it was unreachable, see below), a `quick-toggle` IPC for keyboards | 4–7 | captures cold/warm/switch/toast (private bus)/Reduce Motion/audio/quick, per-frame seam measurement; service-tier 88/0, scaling 90/0, labwc matrix 57/0 (was not running its per-output half), popup smoke incl. quick-toggle, lint ratchets lowered (legacy 30, easing 25, unresolved 2) | Network depth fixed at 648 (content-sized is Phase 17); audio pages still switch instantly (width retargets, pill travels); right panel opens on every screen, as the centre always did |
| 10 Left surfaces | IMPLEMENTED | `ArchMenu` on LEFT_SPILL + SurfaceLifecycle: a PanelWindow on the Overlay layer spanning the left strip (was a PopupWindow placed by an anchor rectangle); content at its finished place, revealed, arriving x −8→0; page changes retarget W/H over `page`; input region = body bounds while open. Spill ridge fade keyed to width, not progress (both spills) | 4–6 | capture open/close; labwc matrix 57/0 (ArchMenu measured mapped at every page, finished bounds inside the window); RTL pins updated (window roots 17, literal-x set) | labwc: the PopupDismiss exception for ArchMenu is probably unnecessary now (Overlay layer) — kept until measured with a real pointer |
| 11 Notifications stack | IMPLEMENTED | `NotificationList.qml`: ScriptModel over stable JS wrappers; add (x +24→0 + fade, notificationShift, emphasizedDecel), remove (x →+40 + fade, state, standardAccel), displaced (y, notificationShift, standard) at the same instant; drag-to-dismiss (1:1, fade to 50 %, 40 % / 600 px/s threshold, fling ≥ 900 px/s, settle without overshoot); Clear all bottom-up 20 ms stagger capped at 100 ms; cards keep a copy of what they show; the scroll fade uses Theme.background | 1, 6 | `check-notification-stack.sh` 13/0 (six rules, mutants); captured arrive/remove on a private bus (`capture-surfaces.sh stack`) | drag and Clear all not captured headless (no pointer); expand/collapse not applicable (cards do not expand) |
| 12 Launcher | IMPLEMENTED | LENS_REVEAL inside the Dashboard: the page is `anchored` (LazyPage: present at once, no travel, leaves by fading in place) so the search field is there and focused from the first frame (immediate focus + the 60 ms fallback); results reveal beneath it as one group — clipped downward from the field while settling 8 px (selection beat, standardDecel) and fading in; rows never animate, only the selection's colour (state); the body retargets 900 → 560 over `page`. The 8 literal colour durations are Motion tokens: the unlisted-literal ratchet is 0 | 1–3 | capture `lens` (switch inside the Dashboard), dash-launcher warm/cold; unified-search 153/0 + 15 mutants, launcher-hover 3/0, popup smoke | no standalone launcher entry exists (brief B.6's second entry point is not wired to any bind); first visit builds the page synchronously (~300 ms at capture scale) — keeps typed keys, kept deliberately |
| 13 Nexus | IMPLEMENTED (motion) | QUIET_SHEET on SurfaceLifecycle (page/emphasizedDecel in, surfaceExitSmall/standardAccel out): scrim with progress × alpha, sheet opacity on the content channel, scale 0.985 → 1 in / 1 → 0.99 out (`closing`, not `open`); window lifetime = `mapped` (no close timer); born-live handled by the lifecycle. NavPane: one travelling selection pill (tint + accent bar) on selection/emphasizedDecel, rows hover-only (text-tinted, not white); pages move in nav order | 2, 3, 7 | captures nexus open/close + `nexus-nav`; nexus smoke clean, colour-page 47/0, display-transaction 48/0 (born-live assertion moved to the lifecycle), rtl, a11y | scrim kept at 0.35 (brief says 0.45 — product look, not changed); nav density (36 px rows, group headers) is Phase 19 IA |
| 14 OSD / toasts / context menus | IMPLEMENTED | OSD CAPSULE on SurfaceLifecycle: fade (state) while dropping 10 px (selection, standardDecel), out rising 6 px; a full-round capsule; value bar SmoothedAnimation (~3 track widths/s) only while up — arriving it shows its value (it used to grow from 0); the entrance never replays while showing; window lifetime = `mapped` (no gone-timer, no private Reduce Motion switch). Context menu PIVOT_POP on the lifecycle: opens once placed, fade (state) + scale 0.97 → 1 (emphasizedDecel) from the corner nearest the pointer (flips when clamped; centre for a keybind open), exit fade only (hover), no scale-down. TrayMenu: the same entrance from the tray icon's corner; no exit (the compositor dismisses a grab popup). Toast: Phase 9 (a RightPanel pane) | 1, 6 | `tests/visual/osd-harness.sh` (the real Osd in the private compositor: show, held key, exit); context capture; sweep green | TrayMenu not captured (needs a tray item headless); `Osd._blocked()` reads `Popups.quickOpen`, which a hover-opened QuickControl never sets, so the OSD can show over the quick controls while they are hovered — needs a per-screen signal |
| 15 Top bar redesign | IMPLEMENTED | 15a: focus-mode collapse on the page beat; the left notch's width animates; the C.6/D.7 hairline (open inset path, suppressed on an attached seam); notch padding 14, left min 140, right min 160; the open pill on the left notch. 15b: IconBtn is ApexIconButton in bar mode (no fill; hover → textPrimary, press → accent + .96; 20 px box, 24 px target); every bar glyph 15 px in its Nerd Font; the colour rule — iconDefault at rest, textPrimary on hover, the accent only for a state (a panel open, a hotspot on, the APEX mark by design); charging/full → success, disconnected → textTertiary; the clock 14/600/tnum in the UI face, no hover accent; spacing 8 in the cluster, 12 around the clock; workspace colours on roles (the shell's last pure white) | 2, 3, 5 | geometry 165/0; seam pixel check; labwc matrix, scaling, rtl, primitives, popup smoke; captures of the bar at rest / network open / power open | notchRadius 15 → 12 (D.1) stays a product call; the band title (D.5) is Phase 17 |
| 16 Component visual refresh | IMPLEMENTED (config controls) | CfgSwitch on ApexPressable (track high surface / accent, thumb travels selection/emphasizedDecel and squeezes .94 on press, no whole-control dip); CfgSegmented pills on ApexPressable (selected = surfaceSelected + accent label at once; per-pill cross-fade, see Deviations); CfgSlider (4 px outlineStrong track, accent thumb with a surface ring, 16 under the pointer, no easing while dragging, SmoothedAnimation for programmatic changes, ring for keyboard focus only); CfgTextField (high surface, soft/strong/accent outline, tertiary placeholder); SmallIconButton on ApexIconButton (now Tab-reachable; a press does not take focus); ApexPressable `focusOnPress` | 2, 3 | a11y 26/0, settings-controls 16/0, slider-wheel 11/0, rtl 37/0, settings-pages 21/0, colour-page 47/0, agent suites + Agent Center smoke; white ratchet 204 → 199; wheel mutant re-anchored | Dashboard cards/StatCard, Kanban, the power menu rows are Phase 17 |
| 17 Page-level redesign | IN PROGRESS (17a) | 17a: the power menu's rows (40 px, no gaps, a glyph column so labels align — they were centred and padded with trailing spaces — the state layer on hover instead of a full accent flood, danger rows tint toward danger, and Tab/Space access: it was pointer-only), its page sized to its rows; the network panel sized to its content between 320 and 648 (WifiTab and NetworkPane report a preferred height); a dash instead of "--%" with no audio device | 16 | captures: power menu, network panel; labwc matrix, popup smoke, colour lint (the row tint's literal and its allowlist entry gone; the swap mutant moved to DashStats) | the band title (D.5), Home tiles, Nexus nav density remain |
| 18 Color/material v2 | IN PROGRESS (18a) | 18a: every translucent-white foreground (199 → 0; 481 sites across 45 files, three Sonnet agents on a written rule set, reviewed) is a palette role — text on textPrimary/Secondary/Tertiary, fills and borders the palette's text at the same alpha — or, where the surface really is fixed (album-art scrim, wallpaper thumbnails, an accent chip), a fixed colour commented as such; decorative empty-state glyphs on outlineStrong; the player card draws on the palette when there is no art (its scrim over nothing was a dark slab in light); the confirm spinner on roles; state distinctions the mechanical pass collapsed restored (three ternaries, the calendar's in/out-of-month days). The Light or dark control is back on the Appearance page, per Colors.qml's recorded plan (withheld in 3790c37 only for the whites); agent-state-test §10 now allows it only while the white count is 0, and check-color-tokens bans the white | 2 | captures of every surface tests/visual can open, dark and light (default wallpaper), incl. every Nexus page, Agents/Tasks/Config tabs, toast, stack, OSD, lock screen; colour lint 22/0 (white 0); agent-state 27/0; full sweep | 18b: shadows / depth for floating surfaces. Not captured in light: the Clock card's Timer and Alarm tabs (TimeInput) |
| 19 IA cleanup | NOT STARTED | Dashboard Config tab vs Nexus | 13 | — | — |
| 20 Hyprland motion | IMPLEMENTED (apex-os `feat/hypr-motion` 256f55ccd, not merged) | appearance.lua: one motion class per change on the shell's own curves — windows in 240 ms emphasizedDecel / out 175 ms emphasizedAccel (popin 87 %), moves direct (standard 200 ms), fades on effects, focus/dim/border on the state beat, app menus 100 ms, workspaces slide 200 ms, other programs' layers on the small-surface beats; a `no_anim` layer rule for `^quickshell$` | 1 | measured in a nested Hyprland 0.56.2 (session scratch `hypr/probe.sh`): on the stock tree 66–90 % of the network panel's footprint was a translucent blend from 240–345 ms into its open (fadeLayersIn over the pour), ≤ 7 % with the rule; `tests/test-apex-hypr-motion.sh` 15/0 (static half in CI, `--verify-config` half with mutants) | not seen: windows and workspaces in motion — the probe cannot map an app window on the nested output. The shell's motion speed / Reduce Motion do not reach Hyprland yet (Phase 21) |
| 21 Accessibility | IN PROGRESS (21a–21c) | 21c: an application's tray menu walks on Up/Down past separators and disabled rows, Return/Enter/Space choose, Right into a submenu, Left/Backspace out; one highlight for the pointer and the arrows, reset on every open. 21b: every transient surface holds the keyboard while it is open — the context menu, ArchMenu, the confirm dialog, and the right panel's network/audio/notification panes (never a toast) are Exclusive while up and None after; QuickControl only when its keybind opened it (hover takes nothing). They were OnDemand or None, and OnDemand is granted only on a click on labwc: measured, Escape and the arrows reached nothing and the menu stayed open. Escape on each, on an ancestor of whatever takes focus. The shared TabSwitcher (Dashboard, network, audio, settings) and the Nexus page list are tab lists — one Tab stop, arrows along the axis, Home/End, a mirrored row reversed, a ring only for keyboard focus, `surfaceSelected` for the pill. The context menu and the power menu walk on Up/Down; the confirm dialog has two real buttons, the confirm focused (so Return still confirms) and its focus shown; the Nexus close button is a real button. 21a: the compositor follows the shell's Motion — speed × Hyprland's own declared values (read back with `hyprctl -j animations`, so apex-os stays the one place they are written), Reduce Motion switches the spatial classes off and caps the fades at 150 ms, scale 0 turns animations off; a per-instance record in $XDG_RUNTIME_DIR stops a shell restart scaling its own push again; a `hyprctl reload` is followed by a re-push. New `motion` capability (Hyprland only). A keyboard audit of every surface (session notes) drives 21b onwards | 3 | 21c: `run-traymenu-keys-test.sh` 6/0 — the real TrayMenu on a fake StatusNotifierItem + DBusMenu (private bus), the application told the right row, mutation-tested; keys are NOT typed there (a grabbing popup opened without a click serial is dismissed at once, measured 62–135 ms, and the harness has no pointer), so the handler→model mapping is a static check. 21b: `run-keyboard-nav-test.sh` 11/0 (mutation-tested: no Tab stop, no arrow step; a keyboard-moved selection kept in view in a scrolling column, and only then — keeping it on open scrolled the live settings column and nav-geometry caught it); live under wtype in the headless compositor (`capture-surfaces.sh keys`): menu Down/End/Escape, Dashboard Tab→Right, Nexus Tab→Down→Escape, power Tab→Down→Escape, confirm opened from the keyboard with its ring, Left to Cancel, Escape; centre and quick controls close on Escape. 21a: `hypr-motion-test.js` 19/0; `run-hypr-motion-sync-test.sh` 6/0 live in a nested Hyprland, restart assertion mutation-tested (compounds to 15 without the record); compositor facade 60/0; backends 47/0 | Next: the panes' own rows (Wi-Fi/Bluetooth/VPN/hotspot, notification cards, clipboard, Kanban, Home cards) are still pointer-only; QuickControl's sliders take no keys; hit targets under 32 px in the panes; Reduce Motion tested as its own mode. The first wtype key of a session is lost (harness: a modifier tap goes first). UpdatePopup has no keyboard path but is dormant on APEX (first-run seeds autoUpdate false) |
| 22 Frame pacing | NOT STARTED | — | 8–14 | bench, `QSG_RENDER_TIMING` | GPU-side unmeasured |
| 23 Shape QA | NOT STARTED | — | 5 | harness | — |
| 24 Stress matrix | NOT STARTED | — | all | — | mixed refresh needs hardware |

## Also shipped on this branch

| Change | Where | Verification |
|---|---|---|
| Password shapes on the lock screen (and the login screen, apex-os `feat/password-shapes`) — one geometric shape per character, chosen by position only, fed the field's length only | `src/components/auth/PasswordShapes.qml`, `Lockscreen.qml`; apex-os `GreetSurface`/`GreetContext`/`apex-greet-wallpaper` | `check-password-shapes.sh` 13/0 (mutants), `run-password-shapes-test.sh` 12/0, `check-lockscreen-a11y.sh` 33/0, apex-os `test-apex-greet-motion.sh` 25/0; captured on the real lock and login screens; visual advisor review folded in |
| Lock screen: a refused password now clears the field (it left the rejected attempt in place) | `Lockscreen.qml` `fail()` | `check-password-shapes.sh` CLEAR rule + mutant |
| Greeter: a refused login's error survives the field being cleared | apex-os `GreetSurface.qml` | `test-apex-greet-motion.sh` §4 + mutant |

## Deviations from the roadmaps

| Where | Roadmap says | Implemented | Why |
|---|---|---|---|
| Segmented control | brief §E: one shared pill travelling between segments | the selection cross-fades per pill on the state beat | CfgSegmented is a Flow that wraps (21 call sites); a pill sliding across wrapped rows reads worse, and a shared pill inside a Flow needs a layout hack or a structural change to every caller |
| Colour roles | brief §C.5: textSecondary mix(T, B, .35), textTertiary .55; fallback checks surfaceHigh only; no tertiary fallback | textSecondary .30, textTertiary .50; the fallback checks surfaceHigh AND surfaceSelected and has a tertiary branch | measured on the twelve real palettes: at .35 the default wallpaper's pale accent put secondary text at 4.38:1 on surfaceSelected, and .55 put tertiary at 2.83:1 on a light scheme — 13 fallbacks fired, one on the shipped default itself (design review 2: a fallback that fires on the default is the formula). At .30 / .50 none fires; the rule stays for unshipped palettes |
| Right notch while a panel is open | brief A.2: the bar widens its notch, `rWidth = W(p)` | the bar's notch stays at its natural width; the panel draws the widened band over it | a moving edge split across two layer surfaces is a frame off itself (measured, D3) |
| Toast while a panel is open | — | not shown; a toast on screen is dismissed when Network or the centre opens; opening the centre drops the queue; a toast arriving while the centre is open is not queued | the body belongs to the open panel; the centre already lists every one of them |
| Toast state | a global `Popups.notificationToastOpen` read by every bar | each screen's panel pushes its own toast state to its own bar | one screen's dismiss used to close every bar's notch |
| Audio popup | RIGHT_POUR | Moves to hang under the right notch as a RIGHT_POUR (its trigger lives in the notch); QuickControl, which is edge-triggered, becomes EDGE_SPILL | The design brief found a pour has no source at mid-edge; the user's instructions and both roadmaps name Audio as RIGHT_POUR. Moving it under its own trigger satisfies both. |
| Dashboard height | — | The finished body's bottom is `notchHeight + dashboardHeight` (was `dashboardHeight` from the screen top) | Pages gain the 40 px the bar used to take, which fixes Home clipping its last tile row (brief §F.1); the setting now means "height below the bar". |
| notchRadius default | brief: 15 → 12 | kept at 15; the bottom corner is now a derived token (14) | A default change is a product call for Andre. |
| Right panel title in the bar's notch | brief §D.5 (optional) | not done; the band's left stays empty | Product call; the brief's own fallback. |
| Launcher page | lazy: built on first visit (the performance baseline) | built in the background, asynchronously, once the Dashboard has finished its first open (`LazyPage.prewarm`) | the first Home → Apps switch spent ~390 ms building the page before anything moved (measured per frame); with the prewarm the switch starts by ~150 ms. The cost is one page kept built after the first Dashboard open, on the page the Dashboard exists to host; every other page stays lazy |
| Phase 0 | "Record videos … frame timing at 60 Hz and one high-refresh target" | Timestamped frame bursts at 3.75x slow motion + CPU/frame bench; no high-refresh capture | Headless outputs are 60 Hz; no high-refresh hardware reachable without opening windows on the user's desk. |

## Resume notes (kept current)

- Committed through 18a and the rest of visual review 2; Phase 20 is on apex-os
  `feat/hypr-motion` (pushed, no PR). Next: Phase 21 (accessibility: Reduce
  Motion as its own mode, including pushing it and the motion speed to Hyprland
  over `hyprctl eval`), then the rest of 17 (band title, Home tiles) and 18b.
- Nested Hyprland: it starts with NO output — `hyprctl output create headless`
  first, `-j` requests, `LIBSEAT_BACKEND=seatd` so its DRM probe never asks
  logind for the live session (memory: nested-hyprland-needs-output-create).
- Capture a light palette: `CAPTURE_PALETTE=<colors.json>` on capture-surfaces,
  capture-lockscreen and osd-harness; the light default-wallpaper palette is in
  tests/fixtures/palettes-matugen-4.2.0.json.
- The toast capture needs a notification and must never send one to the desk:
  `env -u WAYLAND_DISPLAY -u DISPLAY dbus-run-session -- env APEX_CAPTURE_BUS=private
  tests/visual/capture-surfaces.sh OUT toast` (gdbus Notify — this host's
  notify-send does not deliver to the private bus).
- Before each phase commit: EVERY `tests/check-*.sh` (Phase 7 broke two that a
  narrower list missed: agent-center's lazy-page grep and the white-foreground
  ratchet), `tests/check-reduce-motion.sh`,
  `check-wheel-value.sh`, `node tests/{motion,fluid-geometry}-test.js`, the
  qmltestrunner suites (settings-controls, a11y-controls, rtl, lifecycle,
  password-shapes), `run-nav-geometry-test.sh` (16 pre-existing),
  `run-popup-smoke.sh`, `run-nexus-smoke.sh`, then a capture sheet of the surface.
- Staged quickshell harnesses import `./src/services` before `./src`.
- Fable design brief: session scratchpad `fable-brief-1.md` (shape families, tokens,
  top bar, controls).

## Failures that are NOT this branch's (measured on untouched main)

- QuickControl could not be opened at all since the lazy-popup change
  (`cea90b5`, #2): built only by `Popups.quickOpen`, which nothing sets. FIXED in
  9b. (ArchMenu and the wallpaper picker also have strip hover triggers, but
  both are disabled by design — `hoverEnabled: false`, `allowHover: false` — so
  they open by click/IPC only and the lazy build does not affect them.)
- `tests/labwc-matrix-test.qml` built `PopupDismiss` without its required
  `topBar` (since `6b39094`), so the per-output half built no instances and
  failed; CI skips the suite (no labwc there), which is why nobody saw it.
  FIXED in 9b.
- `tests/run-nav-geometry-test.sh`: 16 — the Privacy page lays out no rows at some scales (identical on `17eec38`).
- `tests/run-lockscreen-atspi.sh`: 1 pinned assertion — this machine's Qt now publishes 8 AT-SPI nodes; the pin itself says that is an improvement.
- `tests/run-lockscreen-atspi-shim.sh`: 4 and `tests/run-recovery-atspi-shim.sh`: 2 — the CONTROL / IN-PROCESS pins of the same upstream change (the tree is no longer empty); the identical set fails on untouched main (`99c5ab7`).
- `tests/check-agent-help.sh` fails (`enable`) when `APEX_OS_ROOT` names a stale apex-os checkout; passes against the current CLI.
- apex-os `tests/test-apex-greet-sessions.sh`: 2 — a new `apex-safe-graphics` session the suite does not expect.
- `tests/check-colour-page.sh`: flaky, ~1 run in 3, on main as well.

## Verification log

- 2026-09-26 — Phase 21a: the compositor on the shell's Motion, measured live (see the phase row). Keyboard audit (read-only agent) of every surface: F1 four windows never receive keys (keyboardFocus None: QuickControl, ArchMenu, right panel audio/notifications); F2 the shared TabSwitcher is pointer-only; menus have no arrows; ConfirmDialog's Return confirmed regardless of focus; exemplary: the lock screen and the wallpaper picker.
- 2026-09-26 — Visual review 2, the rest. The stack's cards built while the centre was closed sat jammed at rest: their height counted lines by `visible`, the EFFECTIVE visibility, false while an ancestor is hidden (now the show conditions; a new SHOWN rule in check-notification-stack). A removal closes up a beat late (`removeDisplaced`, contentDelay) with the leaving card gone on the content beat; an arrival waits the same beat. The panel's depth then had to wait too, or it clipped the rising card — as a Behavior its pause read the old depth (the Behavior started before the binding beside it re-evaluated; logged), so the depth retarget is driven. Nexus: the sheet leaves on the scrim's beat and the scrim follows the sheet's channel on close (was 69 % scrim over 15 % sheet at 187 ms; now within 0.02 every frame); radiusXL, page title 18/600. SysTray on Motion tokens and without a bar hover fill (legacy 14 → 12, easing 10 → 8).
- 2026-09-26 — Phase 20 (apex-os): measured the shell's layer being faded by Hyprland before writing the rule; see the phase row.
- 2026-09-26 — Phase 18a: the translucent-white migration. Three Sonnet agents converted 481 sites on a written rule set (text → a text role by alpha; fill/border → the palette's text at the same alpha; a fixed surface → an explicit fixed colour). Reviewed after: three ternaries whose two states landed on one role, and the calendar's in/out-of-month days, restored; four empty-state glyphs promoted from .08–.20 to tertiary text put back to a decorative outline; the player card's unconditional scrim made it a dark slab in light, so it draws on the palette without art. Captured every surface tests/visual can open in both schemes of the default wallpaper: all legible, no QML errors. Light or dark returned to Appearance.
- 2026-09-26 — Accessibility regression found by the sweep and fixed: ApexIconButton (Phase 3) published its glyph as its accessible name, so every bar button reached AT-SPI as a private-use codepoint (the lock-screen and recovery shims' glyph rule, 5 strings). It takes a `label` now and never the glyph; the bar's nine buttons are named for what they do. The shims now fail exactly main's pins.
- 2026-09-26 — Visual review 2 (Fable, over every family's capture sheet): each surface reads as its intended family. Applied here: the context menu's frame was the strip's 6 px border (now a 1 px outlineSoft hairline) and its rows, like the tray menu's, flooded with the accent on hover (now the state layer, textPrimary label); the open pill and the Nexus nav pill share surfaceSelected; Nexus nav rows 36 px on radiusM, selection by fill and colour only (no bold); bar cluster gaps even (Audio's trailing 6 px, the clock's padding); the text-role mixes moved to .30 / .50, so no shipped palette needs a fallback (was 13, including the default); the launcher page prewarmed (see Deviations). Waiting on the Phase 18 migration's files: the stack's add/remove pause and its rest overlap, Nexus's scrim outliving its sheet, SysTray's legacy animation. Full sweep green on an isolated tree (nav-geometry 16 pre-existing).
- 2026-09-26 — Phases 2-3: roles over twelve palettes (13 fallbacks, pinned); primitives suite 10/0; migrated controls captured in Nexus. Found in passing: NavPane opened scrolled past its current page (reveal before layout, pre-existing) and its new pill was missing on first open — both fixed. Two Qt traps recorded in the code: a function named `layer` is shadowed by Item's `layer` group; emitting a derived signal straight from an inherited signal's handler threw.
- 2026-09-26 — Phase 14: OSD captured at 1x through a new harness (entrance, held key tracking without replay, exit); fixed the bar growing from 0 on first show; context menu captured (keybind open, centred). Sweep green.
- 2026-09-26 — Phase 12: launcher tab switch captured (pill, body 900→560, field at once, results group reveal). A suspected first-frame lag on Dashboard opens was measured (bloom width per frame, with and without a first-frame gate): the bloom is visible from ~120 ms either way — the gate was reverted; the earlier "nothing for 300 ms" read was depth-only measurement plus IPC latency. Full sweep green (nav-geometry 16 pre-existing).
- 2026-09-26 — Visual review of Phases 9-10 (Fable, over the capture sheets): all three families read as intended and distinct; no geometry.js change. Applied: pane cross-fade waits `fadeOut` (was a double exposure); Reduce Motion close drops the band at once and fades only the body; the spills' ridge fade on exit only; toast accent/progress clear of the melt corner; the centre's heading left-aligned; open pill tint .22. Found while applying: a flag close of the quick controls waited out the hover grace (animDuration + 200, 1.4 s at capture scale) — now immediate; and the spills' opacity was a binding loop (reading `life.open` beside `life.alpha` runs the lifecycle's open handler, which writes alpha) — SurfaceLifecycle gains a side-effect-free `closing`. Deferred with reasons: text wipes and the open-start hitch are sub-frame at 1x (verify in Phase 22); the depth hairline is Phase 18; a band title (D.5) is now possible since the panel draws the band — a pane-layout change for Phase 17.
- 2026-09-26 — Phase 11: stack arrive/remove captured; four silent ListView failure modes found by measurement (replaced-array model, destroyed-QObject model value, content-sized view releasing exits, untargeted nested exit animations) plus a polish-time height; all guarded by check-notification-stack.sh. Full sweep green.
- 2026-09-26 — Phase 10: LEFT_SPILL captured open/close (horizontal extrusion, then unfold; fold, then retract); the close no longer ends in a translucent box (fade keyed to width). Full sweep green.
- 2026-09-26 — Phase 9b: audio pane and EDGE_SPILL quick controls captured; full `check-*.sh` sweep, node suites, headless suites green (nav-geometry 16 pre-existing); RTL window-root pin 15 → 16 (QuickControl is a PanelWindow).
- 2026-09-26 — Phase 9a: one right panel; seam measured per frame on cold/warm open and close and both pane switches — exact after moving the band into the panel (was 35-77 px off with a Canvas bar, then one-frame ledges with a Shape bar); toast captured on a private bus; Reduce Motion close fades the finished shape. All `check-*.sh` green, node suites green, headless suites green except nav-geometry's 16 pre-existing. Fixed two Phase 7 regressions found by the full sweep.
- 2026-09-26 — Phase 7: shared tab pill + directional pages on the Dashboard; forward/back tab sheets reviewed (pill lands ~290 ms, no pop); suites as in the phase row.
- 2026-09-26 — Phase 8a: Dashboard on CENTER_BLOOM; cold open no longer squeezes the page; lint 22/0 (legacy 40, easing 35, unresolved 4); popup smoke, nexus smoke, scaling 88/0; nav-geometry 16 (pre-existing).
- 2026-09-25 — password shapes (both screens), tokens, four shape families (geometry 129/0), Phase 1 motion + migration, lifecycle.
- 2026-09-25 — Phase 0: baseline captured from `17eec38`; renderer bench (idle 0.63, shape 0.80, geometry 0.93, canvas 2.28, busy-control 2.37 ms/frame).
