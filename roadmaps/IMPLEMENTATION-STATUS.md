# APEX Shell UI/UX redesign — implementation status

Working tracker for the coordinated implementation of
`APEX-Shell-Master-UIUX-Roadmap-v3.md` (engineering order, lifecycle, performance,
QA) and `APEX-Shell-Fluid-Visual-Design-Roadmap-v1.md` (what it looks and moves
like). It is not a third roadmap: it records where each phase is, what it touched,
how it was verified, and every place the implementation deliberately departs from
the text.

Branch: `feat/uiux-redesign` (from `origin/main` @ `17eec38`). Worktree:
`/var/tmp/apex-work/wt-uiux-shell`.

Statuses: NOT STARTED · IN PROGRESS · BLOCKED · IMPLEMENTED (built, tests pass) ·
VERIFIED (built, tests pass, visually reviewed at 1x and slow motion, scrutinised).

## Decisions that shape everything below

| # | Decision | Evidence / reason |
|---|---|---|
| D1 | Fluid geometry renders with `QtQuick.Shapes` + CurveRenderer from a path built each frame out of named parameters; no MSAA layer. | Measured: Shape +0.17 ms/frame vs Canvas +1.65 ms/frame (`tests/visual/bench-renderer.sh`, BASELINE §3). |
| D2 | The geometry itself is a plain-JS module (family, progress, params → path + bounds + content clip), unit-tested with node like `agentstate.js`. | Continuity, no self-intersection and monotone bounds become assertions a CI runner without a compositor can check. |
| D3 | Bar and popup stay separate layer surfaces. One lifecycle object per surface per screen owns `progress`. **Revised in Phase 9:** no moving edge is shared between two windows — the surface draws every part of the notch that moves (band, shoulder, a cover over the bar's own corner) and the bar draws only what never moves. At progress 0 the surface's shape over the notch is the bar's own, so map/unmap is invisible. | Measured: with the bar widening in step from the same integer function, the two layer surfaces presented their frames independently and the notch sat one frame off the body (35-42 px ledges on close/cold open). With the band in the popup window, every frame of every sequence measured seam-exact. The Dashboard already covered its notch the same way. |
| D4 | `SeamlessBarShape` moved off Canvas in Phase 9 (earlier than planned) onto `geometry.js barSilhouette` + CurveRenderer, same corners and rounding. | Measured: the Canvas (8x MSAA layer) repainted a frame or more behind its inputs — up to 77 px of notch lag mid-pour — at 2.28 vs 0.80 ms/frame. Only one Canvas changed, per §27.2's intent. |
| D5 | Motion is its own singleton (`Motion`, registered once in `src/qmldir`), not tokens inside `ThemeSet`. | `tests/lib/theme-stub.sh` regex-parses ThemeSet property declarations; motion is not a per-output quantity. |
| D6 | Roadmaps copied into `roadmaps/` in apex-shell. | `ROADMAP/` above the repos is not under version control. |
| D7 | **Surfaces move on springs, not curves** (2026-09-26, Andre: *"everything goes way too quick and doesn't feel liquid and fluid, i want real apple level animation"*). A surface's body is up to three coupled springs (lead / body / trail) with velocity carried through every reversal; geometry takes their values and their FLOW (speed relative to a nominal peak) and bends edges by it. Durations are perceived and ~1.7x longer for anything that moves a surface; feedback (hover, press) stays brief. Spring numbers are design decisions taken without Andre — he is the judge of the feel. | Closed-form spring (`theme/spring.js`, `tests/spring-test.js` 20/0: theory's overshoot, frame-rate independence, velocity kept through a reversal). Apple HIG motion page (Andre sent it): brief feedback, cancellable motion, realistic response. Fable's spec (session record) for the per-class numbers, adopted with slightly longer responses. |

## Phase map

| Phase | Status | Files / components | Depends on | Tests / artifacts | Risks |
|---|---|---|---|---|---|
| 0 Baseline | IMPLEMENTED | `roadmaps/baseline/BASELINE.md`, `sheets/`, `tests/visual/{capture-surfaces.sh,contact-sheet.py,bench-renderer.*}`, `tests/lib/headless.sh` (`HEADLESS_WLR_RENDERER`) | — | capture + bench reproducible | OSD/notifications/lock/mixed-refresh not capturable headless without a source; recorded as gaps |
| 1 Motion architecture | IMPLEMENTED | `src/theme/{Motion.qml,motion.js,anim/Motion{Color,Fade,Move}.qml}`, `SettingsService` (motionSpeed/motionScale, animDuration migrated by ratio), `LayoutPage`, `tests/check-reduce-motion.sh` (the motion lint), `tests/motion-test.js` | 0 | motion-test 107/0; lint 22/0 incl. self-tests; 403 literals → 8 (AppLauncher) + 2 allowlisted countdowns; 0 ungated loops | legacy `Theme.animDuration` (40 sites) and named easings (35) remain in the surfaces not yet on SurfaceLifecycle — ratchets |
| 2 Visual tokens | IMPLEMENTED | `ThemeSet`: radius roles XS–Full, `notchShoulder`/`notchBottom`, spacing scale, control heights (compact/standard/comfortable/prominent, rows, tile, hit sizes), type-role sizes; `roles.js` + `Colors`/`Theme`: surface roles (base/raised/overlay/high/selected, hover/pressed layers), accentContainer/onAccentContainer/accentText/onAccent, outlines + hairline, text primary/secondary/tertiary, icon default/active; font families (ui = fontconfig sans → Noto Sans, mono, icon = JetBrainsMono Nerd Font) | 0 | check-scale-tokens 50/0; `color-roles-test.js` 16/0 over the twelve matugen 4.2.0 palettes (fixture with provenance) | tokens are defined, not yet adopted — adoption is Phases 3, 15, 16 (and the 211 translucent whites, 18) |
| 3 Interaction primitives | IMPLEMENTED (core) | `src/components/controls/`: `ApexPressable` (press dip to pressScale on pressIn/pressOut, Space/Return replay it and activate, a state layer `tint(base)` / `stateLayer()` over any surface, focus ring only for keyboard-given focus — tracked, since Qt Quick 6.10 has no Item.focusReason — separate `hitMargin`, disabled at .38), `ApexFocusRing` (2 px accent at radius + 2, 2 px gap), `ApexIconButton` (bar mode: no fill, 24 px target). Migrated: CfgButton, CfgTile, ProfileButton (APIs unchanged; roles instead of translucent whites) | 1, 2 | `run-controls-primitives-test.sh` 10/0; a11y 26/0, settings-controls 16/0, rtl 37/0, slider-wheel 11/0 (staged via the new `stage_roles`); white-foreground ratchet 211 → 204 | IconBtn (bar) moves in Phase 15; CfgSwitch/CfgSlider/CfgSegmented and the ApexToggle/ApexSlider/ApexSelectionPill wrappers are Phase 16 |
| 4 Parametric shape engine | IMPLEMENTED | `src/shapes/fluid/{geometry.js,FluidShape.qml}`, `tests/fluid-geometry-test.js`, `tests/visual/fluid-harness.{sh,qml}` | 1 | geometry suite 129/0; harness sheets reviewed, joins zoomed | GPU cost of CurveRenderer unmeasured |
| 5 Shape families | IMPLEMENTED | CENTER_BLOOM, RIGHT_POUR (+ `rightPourWidth`; now draws the notch band, shoulder and corner cover), LEFT_SPILL, EDGE_SPILL (right); `barNotch`, `barSilhouette` | 4 | geometry suite 147/0: per-family sweeps, character checks, pour-at-0 equals the bar notch pixel for pixel, corner cover, cover clear of the icons (mutants caught) | — (checked 2026-09-26: CAPSULE is the OSD, PIVOT_POP the context and tray menus, QUIET_SHEET the Nexus sheet, LENS_REVEAL the launcher, STACK_REFLOW the notification list — each built with its surface, as planned) |
| 6 Surface lifecycle | IMPLEMENTED | `SurfaceLifecycle.qml` (linear progress on open, fastDecel on close, content delay 40/in 130/out 70); under Reduce Motion a close holds the finished shape while it fades (brief B.10); a close+reopen in one tick is a no-op. Dashboard and the right panel migrated | 1 | lifecycle suite 15/0 | Finished 2026-09-26: the last five transient surfaces — the confirm dialog, the display confirmation, the update popup, the window switcher and the screen-recording options — are on `components/DialogLifecycle.qml` (SurfaceLifecycle with the Nexus sheet's modal timings, one preset instead of five copies): each maps its window until its exit has finished and holds the keyboard on `life.open`, not `visible`. They mapped on a flag with no motion, unmapped on a 20 ms timer, or (the switcher) had a scrim fade-out that never showed because the window went first; the legacy-duration ratchet drops 5 → 4. The confirm dialog cannot be driven from a harness (its confirm takes focus on open, and its action authenticates, then logs out or powers off), so check-reduce-motion gates all five statically — maps from a lifecycle, keys only while open — self-tested both ways. PopupLayer, Border and PopupDismiss are not surfaces. | 
| 7 Connected navigation | IMPLEMENTED | `TabSwitcher.qml`: one pill per switcher travels to the chosen tab (selection token, emphasizedDecel; armed after first placement); tabs draw hover only. `LazyPage.qml`: directional enter/exit (pageTravel, page token), interruption-safe. Dashboard derives direction from tab order via `shownPage` | 1, 3 | nav-geometry 4398/16 (pre-existing), wheel 16/0, settings-controls 16/0, a11y 26/0, rtl 37/0, popup smoke clean; `capture-surfaces.sh … tabs` sheets fwd/back | NavPane done in Phase 13 |
| 8 Dashboard v2 | IMPLEMENTED (motion) | `Dashboard.qml` on CENTER_BLOOM + SurfaceLifecycle, content at final layout under the bloom clip; `TopBar.cWidth` no longer widens for it; `SeamlessBarShape` on the notch tokens, whole-pixel positions | 4–7 | captures cold/warm at 2.5x (`tests/visual/capture-surfaces.sh`), shoulder zoom; popup/nexus smoke, scaling 88/0 | page redesign is Phase 17 |
| 9 Right quick surfaces | IMPLEMENTED | 9a: `RightPanel.qml` — Network, the notification centre, audio and the toast as panes of ONE RIGHT_POUR surface per screen (`NetworkPane`, `NotificationsPane`, `AudioControl`, `NotificationToast` as Items); clock `TopBar.rightLife`, starting only once the panel is built; the bar's notch never moves (the panel draws the band); pane switch keeps the body open and retargets W/D over `page`, panes cross-fade; `OpenPill` on the owning glyph (wifi/bt/vpn/hotspot, speaker, bell), no ▾; network tabs arrive directionally. 9b: Audio moved from the right strip to a pane under its own notch trigger (`AudioPopup` removed); `QuickControl` on EDGE_SPILL as a PanelWindow spanning the right strip, built by the strip hover (it was unreachable, see below), a `quick-toggle` IPC for keyboards | 4–7 | captures cold/warm/switch/toast (private bus)/Reduce Motion/audio/quick, per-frame seam measurement; service-tier 88/0, scaling 90/0, labwc matrix 57/0 (was not running its per-output half), popup smoke incl. quick-toggle, lint ratchets lowered (legacy 30, easing 25, unresolved 2) | Network depth fixed at 648 (content-sized is Phase 17); audio pages still switch instantly (width retargets, pill travels); right panel opens on every screen, as the centre always did |
| 10 Left surfaces | IMPLEMENTED | `ArchMenu` on LEFT_SPILL + SurfaceLifecycle: a PanelWindow on the Overlay layer spanning the left strip (was a PopupWindow placed by an anchor rectangle); content at its finished place, revealed, arriving x −8→0; page changes retarget W/H over `page`; input region = body bounds while open. Spill ridge fade keyed to width, not progress (both spills) | 4–6 | capture open/close; labwc matrix 57/0 (ArchMenu measured mapped at every page, finished bounds inside the window); RTL pins updated (window roots 17, literal-x set) | labwc: the PopupDismiss exception for ArchMenu is probably unnecessary now (Overlay layer) — kept until measured with a real pointer |
| 11 Notifications stack | IMPLEMENTED | `NotificationList.qml`: ScriptModel over stable JS wrappers; add (x +24→0 + fade, notificationShift, emphasizedDecel), remove (x →+40 + fade, state, standardAccel), displaced (y, notificationShift, standard) at the same instant; drag-to-dismiss (1:1, fade to 50 %, 40 % / 600 px/s threshold, fling ≥ 900 px/s, settle without overshoot); Clear all bottom-up 20 ms stagger capped at 100 ms; cards keep a copy of what they show; the scroll fade uses Theme.background | 1, 6 | `check-notification-stack.sh` 13/0 (six rules, mutants); captured arrive/remove on a private bus (`capture-surfaces.sh stack`) | drag and Clear all not captured headless (no pointer); expand/collapse not applicable (cards do not expand) |
| 12 Launcher | IMPLEMENTED | LENS_REVEAL inside the Dashboard: the page is `anchored` (LazyPage: present at once, no travel, leaves by fading in place) so the search field is there and focused from the first frame (immediate focus + the 60 ms fallback); results reveal beneath it as one group — clipped downward from the field while settling 8 px (selection beat, standardDecel) and fading in; rows never animate, only the selection's colour (state); the body retargets 900 → 560 over `page`. The 8 literal colour durations are Motion tokens: the unlisted-literal ratchet is 0 | 1–3 | capture `lens` (switch inside the Dashboard), dash-launcher warm/cold; unified-search 153/0 + 15 mutants, launcher-hover 3/0, popup smoke | no standalone launcher entry exists (brief B.6's second entry point is not wired to any bind); first visit builds the page synchronously (~300 ms at capture scale) — keeps typed keys, kept deliberately |
| 13 Nexus | IMPLEMENTED (motion) | QUIET_SHEET on SurfaceLifecycle (page/emphasizedDecel in, surfaceExitSmall/standardAccel out): scrim with progress × alpha, sheet opacity on the content channel, scale 0.985 → 1 in / 1 → 0.99 out (`closing`, not `open`); window lifetime = `mapped` (no close timer); born-live handled by the lifecycle. NavPane: one travelling selection pill (tint + accent bar) on selection/emphasizedDecel, rows hover-only (text-tinted, not white); pages move in nav order | 2, 3, 7 | captures nexus open/close + `nexus-nav`; nexus smoke clean, colour-page 47/0, display-transaction 48/0 (born-live assertion moved to the lifecycle), rtl, a11y | scrim kept at 0.35 (brief says 0.45 — product look, not changed); nav density: rows stay 36 px, group headers landed in 19b |
| 14 OSD / toasts / context menus | IMPLEMENTED | OSD CAPSULE on SurfaceLifecycle: fade (state) while dropping 10 px (selection, standardDecel), out rising 6 px; a full-round capsule; value bar SmoothedAnimation (~3 track widths/s) only while up — arriving it shows its value (it used to grow from 0); the entrance never replays while showing; window lifetime = `mapped` (no gone-timer, no private Reduce Motion switch). Context menu PIVOT_POP on the lifecycle: opens once placed, fade (state) + scale 0.97 → 1 (emphasizedDecel) from the corner nearest the pointer (flips when clamped; centre for a keybind open), exit fade only (hover), no scale-down. TrayMenu: the same entrance from the tray icon's corner; no exit (the compositor dismisses a grab popup). Toast: Phase 9 (a RightPanel pane) | 1, 6 | `tests/visual/osd-harness.sh` (the real Osd in the private compositor: show, held key, exit); context capture; sweep green | TrayMenu not captured (needs a tray item headless); `Osd._blocked()` reads `Popups.quickOpen`, which a hover-opened QuickControl never sets, so the OSD can show over the quick controls while they are hovered — needs a per-screen signal |
| 15 Top bar redesign | IMPLEMENTED | 15a: focus-mode collapse on the page beat; the left notch's width animates; the C.6/D.7 hairline (open inset path, suppressed on an attached seam); notch padding 14, left min 140, right min 160; the open pill on the left notch. 15b: IconBtn is ApexIconButton in bar mode (no fill; hover → textPrimary, press → accent + .96; 20 px box, 24 px target); every bar glyph 15 px in its Nerd Font; the colour rule — iconDefault at rest, textPrimary on hover, the accent only for a state (a panel open, a hotspot on, the APEX mark by design); charging/full → success, disconnected → textTertiary; the clock 14/600/tnum in the UI face, no hover accent; spacing 8 in the cluster, 12 around the clock; workspace colours on roles (the shell's last pure white) | 2, 3, 5 | geometry 165/0; seam pixel check; labwc matrix, scaling, rtl, primitives, popup smoke; captures of the bar at rest / network open / power open | notchRadius 15 → 12 (D.1) stays a product call; the band title (D.5) is Phase 17 |
| 16 Component visual refresh | IMPLEMENTED (config controls) | CfgSwitch on ApexPressable (track high surface / accent, thumb travels selection/emphasizedDecel and squeezes .94 on press, no whole-control dip); CfgSegmented pills on ApexPressable (selected = surfaceSelected + accent label at once; per-pill cross-fade, see Deviations); CfgSlider (4 px outlineStrong track, accent thumb with a surface ring, 16 under the pointer, no easing while dragging, SmoothedAnimation for programmatic changes, ring for keyboard focus only); CfgTextField (high surface, soft/strong/accent outline, tertiary placeholder); SmallIconButton on ApexIconButton (now Tab-reachable; a press does not take focus); ApexPressable `focusOnPress` | 2, 3 | a11y 26/0, settings-controls 16/0, slider-wheel 11/0, rtl 37/0, settings-pages 21/0, colour-page 47/0, agent suites + Agent Center smoke; white ratchet 204 → 199; wheel mutant re-anchored | Dashboard cards/StatCard, Kanban, the power menu rows are Phase 17 |
| 17 Page-level redesign | DONE (17a–17o; D.5 and nav density are Andre's / Phase 19) | 17o, the Phase 17 review (Opus, adversarial, read-only; every finding checked here before acting): the claims above marked [corrected in 17o] were false and are fixed, not just reworded — Quick Settings' SHADER heading is the shared SectionLabel; Keybinds' conflict warning is the caption size in full danger (it was 9 px at .75 — a conflict is information), its keycaps and "Conflicts with" the caption role in Theme.fontMono; CfgSlider's readout and the Home brightness readout are the readout treatment (typeMono, textSecondary); Recovery's seven 9 px texts, among them the factory reset's "NOT backed up" line, are captions; Kanban's "Add time" too; the Agents card's buttons and the Kill Switch are the 32 px action height; the empty state's command chip is a read-only TextEdit that can be selected and copied. Two timestamp defects in 17n: a card that arrived while the centre was open read as "in the future" and showed no time until the next 30 s tick (the clock is now re-read when the list changes, and a few seconds ahead reads "now" — 15 formatter cases); a replaced notification kept its first arrival time (the service refreshes it before the already-tracked return, and a card whose summary or body changes takes the new time). The clock ran whenever the pane was loaded (`visible` stays true after the panel closes); it runs only while the centre is open now. The toast drew a 3 px urgency bar in the accent for every Normal notification while the card (17i) marks Critical alone — the toast now draws its bar for Critical only, so the two surfaces say the same thing again; the card's dead urgencyColor went. Firewall's hero detail wraps (its tail is the command the line exists to show, and it elided at the sheet's width). The power-profile choices (ProfileButton) take the toggle grammar: surfaceSelected + accentText when chosen, the action style otherwise, no border, radiusS (they were bordered pills flooded with the accent). The Clock card's "No alarms set / Tap + to add one" — touch words, in the tertiary role reserved for placeholders — is the shared empty state; TimeInput's HH/MM labels are captions (9 px). Found while checking the profile choice in both schemes, and worse than anything the review listed: Theme.onAccentContainer had never been bound. A QML property called on<X> declared beside a property called x is taken for handler syntax — Colors has accentContainer — so the role stayed an invalid colour (measured valid: false) and drew BLACK: every "on" Home tile's label and glyph and the lifecycle chip's text sat black on the dark accent container from Phase 2 until now, and no capture review caught it (the light scheme hides it; dark was read as "dark text on olive"). Renamed textOnAccentContainer (roles.js keeps its key); check-color-tokens fails any on<X> property declared beside an x, self-tested on a copy of Colors.qml with the old name. onAccent and every onScreen are safe (no accent or screen property beside them). Most important, a harness finding: 24 runners that do not use lib/headless.sh were on the desktop's own session bus — see "Also shipped". Recorded, not changed: a wrapped lifecycle sentence centres on the 18 px chip; the row hover now reaches under the scrollbar lane; an off Home tile (surfaceOverlay) is a 3 % step from its card (surfaceRaised). 17n, notification timestamps and a DND leak: each card says when it arrived — "now", "12 min", a clock time today, "Yesterday", then a date — in the caption role on its app line's trailing end (visual roadmap §25). NotificationService keeps the arrival time by id (the server does not, and a Notification takes no new properties from JS); the card snapshots it, so a closed card keeps it too; the wording is `notifications/notiftime.js`, pure, driven by `tests/notif-time-test.js` (14 cases: the minute and hour boundaries, midnight, yesterday, a month change, nothing for a missing or future time), which check-notification-stack now runs. The clock ticks every 30 s only while the centre is on screen with something in it. The time is a child of the app line rather than a sibling, so the card's height still counts exactly the lines it did (the HEIGHT and SHOWN rules). Found on the way: a notification that arrived under Do Not Disturb never left the list when it closed — the close handler was connected after the DND early return — so the centre kept cards for things already gone; connected first now, and check-notification-stack asserts the order, self-tested on a swapped copy (with a guard that the mutation applied). 17m, one action shape: CfgButton — the button of all nineteen settings surfaces — is the shape the network panes, the Hotspot form and the Agents card use [corrected in 17o: the Agents card's buttons and the Kill Switch were 28 px until 17o]: the high surface at radiusS, no border, the token height for a button (controlStandard 32, ThemeSet's own comment for "buttons, inputs, row actions"), the label in the caption role. It was the raised surface (.05 — near-invisible on the light sheet once the outline goes, which is why the fill moved up to surfaceHigh rather than the border simply going) inside an outlineStrong border at 28 px. The danger variant keeps its border: borders mean an input, the focus ring, or a confirmation that destroys something. The Home tiles' sublabel is the caption role in the mono face (it was 10 px) and the Kanban board's last sub-11 texts — the column count, the date picker's weekday and day numbers, the urgency and due pickers, the edit hint — are the caption role [corrected in 17o: not so — Kanban's "Add time" stayed at 10 and the Home brightness readout at 9 until 17o; the 7 px ▾/▴/←/→ glyphs and the tile label's 9 px shrink floor remain, by design] (S1: nothing under 11 but the tile sublabel, and now not that either, since 11 fits its reserved line). 17l, empty states, readouts and the Keybinds edge: `components/EmptyState.qml` is the one empty state — a 28 px glyph in outlineStrong (decoration, no role), the title in the body role, one hint sentence in the caption role, and the command, where there is one, as a mono chip [corrected in 17o: it was a plain Text, not copyable; a read-only selectable TextEdit since] instead of run into the prose; centred in a surface's free area, or `inline` on a settings page's content edge without the glyph. Adopted by the Agents panel's two states (the runtime is off / no sessions — 42 px glyphs in subtext with the command inside the hint), Wi-Fi's and Bluetooth's "nothing found", VPN's "no WireGuard connections" (its bordered code chip) and Appearance's "no wallpapers" (a centred tertiary line, now inline on the edge with what to do). Display's and the pairing pages' empty rows stay CfgRows: the inline empty state IS a non-hovering row's label and description, and a second component drawing the same thing would be the duplication §28 is against. Readouts in a row's slot (P2.17) are on the mono role in Theme.fontMono — a command to copy (Firewall's start command, Recovery's rollback) in accentText, a value (the config provider, the pairing countdown, the agent sandbox default, which keeps the danger tone for unrestricted) in textSecondary — where they were 11–13 px, bold or not, in Theme.active or Theme.text; every "JetBrains Mono" literal on the settings pages is Theme.fontMono [corrected in 17o: true of pages/ only — KeybindsPage and CfgSlider kept theirs until 17o]. Keybinds' lifecycle line and commit bar sit on CfgScroll's banner line (12 in, 8 down) like the other fifteen pages instead of at x 0, 12 px left of its own list — the RTL width-compensated bucket is now empty (knowingly). Keybinds keeps its own Flickable (its keep-in-view and staging logic are why it has one) but takes CfgScroll's geometry: the flick 4 in, each row's surface reaching 8 past its text, so the text sits on the same 12 px edge (it was at 22) — the rows clip themselves for the capture-height animation, so the surface moves out rather than a hover bleeding past a clip; its group headings, the last copy of the old 9 px accent-at-.55 heading on the settings pages [corrected in 17o: Quick Settings' SHADER was another] (2.37:1 on the light sheet), are the shared SectionLabel in CfgSection's slot. The Agents panel's first-run card (P2.13) is a surface, not a box: surfaceRaised at radiusL with no border (a .05 fill in a .09 border), the title in the body-strong role and the body at 12 in textSecondary (10 px in subtext), Read the guide in the one action style and Got it a text button (Read was flooded with the accent). 17k, the status pages' heroes and the last inset: `components/config/StatusHero.qml` is the one hero (glyph in the state's colour, the answer in the pane-title role, the technical line in mono bounded by whatever sits at the trailing end, which it wraps or elides instead of running under), adopted on Firewall (it was the one hero in a tinted bordered card — state said by fill, border, glyph and words), Privacy and Closing the Lid; Recovery's hero takes the same geometry written out in its own file, because its P2-003 accessibility contract is read out of that file by brace depth (check-recovery-a11y, 34/0; the mutation suite's anchors untouched). The glyph stays at 28, not Fable's 20: beside a two-line block 20 read as a bullet, and Recovery's mutant R3 anchors on that line. The remaining 10 px insets written as `theme.px(10)` — 33 sites the numeric-x gate cannot see — were classified by parent: the ones bare on the page (descriptions, notes, the lid report's lines, Agents' claim lines and session rows, the pairing and device cards) move to the content edge; the one inside a bordered card (a paired device's text) is padding and stays. Recovery's component rows lose a .03 hover that lit and did nothing (CfgRow's rule). The Hotspot pane joins the other three (outside 17d's brief, caught in review): no box round the note or the form, one state label instead of a dot and a coloured word, fields in CfgTextField's treatment, Save in the one action style on the form's trailing edge, and shown only after an edit (loading the saved file fired textChanged and marked an untouched form unsaved, so Save showed on every open — textEdited now); `capture-pages.sh` now captures it. A run with StatusHero unregistered in `components/config/qmldir` failed every settings page while every check-*.sh passed — none of them loads QML. 17b.3, the settings pages' free-standing blocks on the same edge: the SystemStats key/value list is plain rows (key in textSecondary 600, value beside it in textPrimary; it was zebra pills 10 px inside the rows), and Appearance's mode and scheme pills, Data's disk bars and folder chips, Misc's compositor pills and their description lines all start on the content edge with the rows around them (six `x: 10; width: parent.width - 20` blocks, three `leftPadding: 10` lines and four `anchors.leftMargin: 10` — the palette swatches, the wallpaper strip, the About row and Update now — which had to move together or the pills sat 10 px off their own sentence). Every remaining 10 px text on the settings pages [corrected in 17o: in pages/ — Keybinds' 10 px and Recovery's seven 9 px texts stayed until 17o] — 52 descriptions, details, chips and mono readouts — is the ladder's caption role (11). The RTL width-compensated bucket shrinks knowingly (MiscPage 3 → 2). 17d, the network panes (brief to Sonnet, reviewed here): rows are list items, not cards — no border at rest, the connected row alone carries surfaceSelected, hover the surface's hover tint (each row had a border whose colour meant connected, needs-a-password or about-to-be-removed, a third status channel beside the subtitle and the glyph); ONE action style — Connect, Disconnect, Forget, Pair, Remove, Cancel are all labelled 32 px buttons on the state-layer tint in textPrimary (Disconnect and Forget were bare 28 px glyphs, Connect an accent pill with a border, Bluetooth's connected state a filled pill with a dot that repeated the subtitle); VPN rows gained that button too (the agent's pass took their status dot and left an idle row with no visible verb — caught in review), and on the selected row the buttons sit on the sheet colour, because in light surfaceHigh and surfaceSelected converge and the buttons vanished into their own row (caught in the light capture); the one destructive action per pane is the confirmation's own button, on Theme.dangerFill like every other confirm-to-delete in the shell; the header trios are borderless with the state layer, keeping only genuine state colour (accent while the radio is off, a danger preview on the power button, a dimmed accent while scanning). VPN reads ACTIVE → AVAILABLE → TUNNEL, sing-box last in its own section (the list opened on sing-box whatever was connected); the Kill Switch is a real toggle (checkable, surfaceSelected + accentText when on). Bluetooth, VPN and Hotspot report a preferred height like Wi-Fi, so the panel sizes to them too instead of a fixed 648. netpanes-keys follows the new order (Tab ×3 lands on the active tunnel; the asserted nmcli calls are unchanged). 17j, three settings pages' defects: Recovery's "Components" and "Ways back into this machine" headings showed over zero rows (now each needs its own rows; a generic "hide an empty section" would fall into the effective-visibility trap — a hidden section's rows read invisible, so it could never come back); Lid said its hero sentence twice, 60 px apart (the section's line hides when it is word for word the hero's); Firewall's mono status line was unbounded and ran into Re-check (bounded by the button, elided). 17i, notifications and the player: each notification is a card on the raised surface, 8 px apart (1 px apart, each card's 3 px left urgency bar joined the next into one rule that read as the panel's edge); a Critical one is tinted toward danger instead of a bar (§25); the hover state now shows (it read a HoverHandler's `containsMouse`, which does not exist); the ✕ is a readable glyph that brightens on hover; the header in the pane-title role. The player's visualiser shows only while something plays — at rest its 2 px bars drew a dashed line along the card's bottom that read as a broken border. The clock face, the profile card's height and the calendar's rows are recorded as product calls (Deviations), not changed. 17h, the launcher: the field's placeholder is "Search apps, files and settings" (it was a six-item legend — install · ssh · ~/ · > · ? — where the reader looks to type); the prefixes are one caption line under the list, shown only while the field is empty (visual roadmap §26); the selected row is one surface, no outline (it drew the fill and a 1 px border); descriptions at typeCaption. 17g, the System page and the card primitive: the bottom row is ONE card with two top-aligned columns — Network and Power profile (its three pills now one row) on the left, Disks on the right — where it was three full-height cards, each over 70 % empty (the network rows centred in a narrow one, one disk bar with the card's height beneath it, three pills stacked in the widest); every heading is the shared SectionLabel, left-aligned; upload and download in the same colour (green against the accent said nothing); StatRow values typeBodySmall 600 with tabular figures in textPrimary. StatCard — the card of Home and System — is the raised surface with no border (it was a .04 fill with a border): a card is a surface, not a box. 17f, the Kanban board: one level of box — the column is surfaceRaised with no border (it was a bordered box of bordered cards of bordered buttons), the card surfaceOverlay with one outlineSoft hairline for every urgency (the border was the urgency colour, as were the chip and the ✕: status three times), urgency on the chip alone (its colour at .16, the colour as text, typeCaption); the card's ▾ ← → ✕ carry no fill at rest, only the state layer (✕ tints to danger on hover); the column header typeBodyStrong in textPrimary; the colour emoji 📅 ⏰ and the text ✕ are Nerd Font glyphs in iconDefault. 17e, the Home tiles: the token height (tileHeight 72) instead of 85 % of the width (76), so the fifth row no longer runs through the card's clip mid-label; one surface and one label say on or off (it was the fill, a status dot, the icon colour and the label weight, with a border round each of ten tiles in a bordered card): off is surfaceOverlay, on is the accent container, no borders, no dot; labels at typeCaption shrink to fit a narrow tile (never under 9) rather than eliding; the sublabel line is always reserved, so a row's labels share a baseline. `capture-pages.sh` takes CAPTURE_PAGES to capture a subset. 17b.2, the settings geometry: ONE content edge per page — the page title, the lifecycle line, the section labels and the rows' text all start at the same x (the rows hung 10 px inside the labels), the controls end on the content's right edge, the row hover bleeds 8 px past both into room CfgScroll leaves (its column inset 8 inside a flick 4 from the page), and the scrollbar keeps a lane of its own instead of running over the content. The lifecycle line (P0-023) is a chip in the accent container and the vocabulary's sentence beside it in the caption role — no box (it was a 50 px bordered box on every page); a failed write keeps a danger box, the one thing there that is wrong. CfgRow lights on hover only when it holds a control (prose rows lit up and did nothing — RemotePair 6/6, Blueprint 12/39). NavPane's "Settings" heading is pinned above its scrolled list (it scrolled away on Keybinds and was gone on Misc), in the page-title role. nav-geometry fails exactly the same 16 assertions by name; the RTL banner pin moves 2 → 12 knowingly (still asymmetric — 12 on the reading side, 4 at the lane — so mirroring stays measurable). 17c (Andre: "the how agents and workspaces thing shouldn't always be displayed, but it should be easy to get back to on the agents panel, same with ALWAYS UNRESTRICTED ON, it should be obvious but it shouldn't be a gigantic card taking up real estate"): the Agents panel's top is ONE line — Always Unrestricted as a danger chip on the left while it is on (warning glyph + label; what the card said is its tooltip and accessible description; a click opens the Config page that carries the toggle), the guide as a help button on the right (always there, one click or Tab + Return; its keyboard close still hands the keys back to it). They were a full-width bordered row on every visit and a two-line danger card. The settings page keeps the full banner beside its switch; the first-run card still introduces the guide until dismissed. check-agent-settings' criterion 9 now reads the chip (shown exactly while the default is unrestricted). 17b.1 (from a Fable critique of every page, dark and light, captured with content by the new `tests/visual/capture-pages.sh`; its central finding verified: the Phase 2 type ladder had 0 uses of typeSection/typeBodyStrong/typeHeading/typeMono against 72 raw fs(9) and 124 fs(10)): ONE section label — `components/SectionLabel.qml`, the ladder's typeSection (11/600 uppercase +0.6) in textSecondary — replaces six treatments [corrected in 17o: not all — Quick Settings' SHADER heading kept the old one until 17o]: the settings pages' 9 px accent at .55 (2.37:1 on the light sheet, 6.19:1 now), the Agents list's, the network panes' (two colours in one pane; VPN's TUNNEL changed colour with state the row already shows), the Home card's and AudioControl's private copy; CfgRow on the ladder (label typeBody 13, description and effect note typeCaption 11, readout typeMono 12 in Theme.fontMono). Type only: nav-geometry's failures identical by name, rtl 37/0. 17a: the power menu's rows (40 px, no gaps, a glyph column so labels align — they were centred and padded with trailing spaces — the state layer on hover instead of a full accent flood, danger rows tint toward danger, and Tab/Space access: it was pointer-only), its page sized to its rows; the network panel sized to its content between 320 and 648 (WifiTab and NetworkPane report a preferred height); a dash instead of "--%" with no audio device | 16 | captures: power menu, network panel; labwc matrix, popup smoke, colour lint (the row tint's literal and its allowlist entry gone; the swap mutant moved to DashStats) | the band title (D.5), Home tiles, Nexus nav density remain |
| 18 Color/material v2 | DONE (18a, 18b.1, 18b.2; blur not adopted, by decision) | 18b.2, depth for floating surfaces (visual roadmap §13; spec from Fable, reconciled): `components/Elevation.qml`, one RectangularShadow per floating surface, two levels by what is behind it — popup over the bare desktop (the context menu: blur 20, 4 down, black .40 dark / .22 light) and modal over a scrim (the window switcher, both confirm cards, the display confirmation, the update popup and the settings sheet: blur 40, 12 down, 4 in, .45 / .28). The dark alphas are a starting point: every capture is the pale default wallpaper, where a dark card separates by tone and the shadow does little. Bar-attached surfaces (Dashboard, right panel, toasts, OSD, power menu) cast nothing — a shadow would stop at the join with the bar and show the seam. Every floating surface keeps a 1 px outlineSoft rim WITH the shadow (on a dark card over a dark wallpaper a black shadow separates nothing): the window switcher's was the bar's border colour at the user-set border width, the confirm cards had none, three were a raw text@.08 — all the role now. RectangularShadow is analytic (one quad, no offscreen layer): measured with two new bench-renderer variants, a card resizing every frame costs 0.500–0.550 ms CPU/frame without the shadow and 0.550–0.583 with it (+0.03–0.07; the calibration spin reads +1.7 ms, so the bench measures) — against D1's +1.65 ms for Canvas. Fable's clipping worry for the context menu does not apply: its window is full-screen. Not done, by decision: blur (§13.1 allows the Nexus backdrop "if benchmarking proves it safe"; the sheet now separates by scrim + shadow + rim, and a third technique is the stacking §13 warns against). Recorded for Andre: the four scrims differ — the sheet .35 black, the switcher .45 of the palette's background (contradicting ConfirmDialog's own comment), the update popup .50, the confirm dialog .60 — one value is a product call, like the sheet's .35 vs .45 already recorded. 18b.1, a control on a selected surface: surfaceHigh and surfaceSelected differ by hue alone — 1.01 to 1.13 in luminance on all twelve shipped palettes, dark included — so under a low-chroma accent a button on a selected row vanished into it (17d, light). The fix first proposed, a separation floor between the two roles, was measured and rejected: reaching 1.15 takes the accent mix to .21–.27 on the light palettes, where accentContainer against surfaceSelected falls to 1.00–1.04 (the Home tiles' "on" would vanish instead). Stepping the selected surface toward the text, the way surfaceHigh steps the base, was measured too: separation grows under hover (1.26 → 1.40 → 1.51) but the label pays — textPrimary 6.93 at rest and 5.45 pressed, under the 7.0 target, where an ordinary button gets 9.0 / 8.2. So roles.js gains `surfaceOnSelected` — the sheet colour, the label at full contrast — with a floor (TARGETS.onSelectedStep 1.2; a fallback pushes surfaceSelected to mix(B, A, .22), fired on no shipped palette and self-tested on a weak one), and color-roles-test asserts it at rest (1.25 worst) and hovered (1.11 worst — the state layer moves toward the row), that accentContainer stays apart from selected (1.16 worst; the assertion that would have caught the floor), and textPrimary on the pressed fill (11.35 worst). The network panes' three selected-row buttons use the role instead of a bare surfaceBase. Found on the way, from 17k: check-colour-page's "the profile control is given a width to wrap against" pinned the old `- px(20)` inset and failed once the pills moved to the content edge; the rule is about the width coming from the parent, so the inset is optional now (still fails with no width — checked on a copy). My per-phase static runs were hand-picked lists and missed it; every check-*.sh (33) now runs before a commit. 18a: every translucent-white foreground (199 → 0; 481 sites across 45 files, three Sonnet agents on a written rule set, reviewed) is a palette role — text on textPrimary/Secondary/Tertiary, fills and borders the palette's text at the same alpha — or, where the surface really is fixed (album-art scrim, wallpaper thumbnails, an accent chip), a fixed colour commented as such; decorative empty-state glyphs on outlineStrong; the player card draws on the palette when there is no art (its scrim over nothing was a dark slab in light); the confirm spinner on roles; state distinctions the mechanical pass collapsed restored (three ternaries, the calendar's in/out-of-month days). The Light or dark control is back on the Appearance page, per Colors.qml's recorded plan (withheld in 3790c37 only for the whites); agent-state-test §10 now allows it only while the white count is 0, and check-color-tokens bans the white | 2 | captures of every surface tests/visual can open, dark and light (default wallpaper), incl. every Nexus page, Agents/Tasks/Config tabs, toast, stack, OSD, lock screen; colour lint 22/0 (white 0); agent-state 27/0; full sweep | 18b: shadows / depth for floating surfaces. Not captured in light: the Clock card's Timer and Alarm tabs (TimeInput) |
| 19 IA cleanup | DONE (19a, 19b) | 19b, the settings navigation grouped (the Phase 13 deferral, "nav density … is Phase 19 IA"): PageRegistry pages carry a `group` and are listed in group order — Look & feel (Appearance, Layout, Display), Input (Input, Keybinds), Privacy & agents (Privacy, Firewall, Agents), Devices (Pair, Paired), System (Data, Lid, Gaming, Recovery, Blueprint, Misc) — and NavPane draws the shared SectionLabel above each group's first row, in CfgSection's slot. Sixteen pages in one column read as a pile; five sections read as a map. The selection pill and keep-in-view target the row inside each slot, so the pill never covers a heading and revealing a group's first page brings its heading too. Rows stay 36 px; the list scrolls as it did. Appearance stays the first page. 19a, one home for settings (an inventory first — every setting on more than one surface, every route to advanced configuration — then its consequences). **The Dashboard's Config tab is gone**: it was a second host of all sixteen PageRegistry pages — two instances of every page, with the staged-keybind bug (two `_pending` maps) already on record from it — inside the operational Dashboard, which the roadmap says is "not a second settings app". The Dashboard has five tabs (Home, System, Agents, Tasks, Apps). Nexus's body is now `nexus/SettingsHost.qml` (navigation, header, page stack), the one host of the pages, and an Item, so settings-staged lays out the real host instead of ShellConfig. `dashboard-config` is kept as a compatibility name that opens Nexus — SUPER+C, apex-os's `apex-labwc-keybinds` ("dashboard-config": "settings") and users' keybinds.json all name it; its keybind label is "Settings". nav-geometry loses the Config tab's 16-row vertical column and its 360 px page pane (Nexus's NavPane is a different component, and the audio popup's 3-row column stays covered): its failures went 16 → 8 by name — the Privacy page's pane=360 pair at each scale went with the pane, NOT fixed; the same pair stands at 560. popup-smoke builds every page (the `nexus pages` list, 16) where it built eight hand-listed ids. Found by the inventory and fixed first: a saved hotspot password never reached the Home tile (it read hotspot.json once, at startup — the tile now watches the file); the recording strip's choices were saved only when a recording started (now on change, like the Data page). Surfaces by role — **Glance**: the OSD, the bar's status glyphs, toasts. **Quick Action**: the network panel (Wi-Fi, Bluetooth, VPN, Hotspot), audio (QuickControl, the audio popup), the notification centre, the power menu, the Home quick-settings tiles, the wallpaper picker, the recording strip. **Command**: the launcher (the Apps tab) and its search, the clipboard history, the window switcher, the desktop context menu. **Management**: Nexus. **Operational** (the Dashboard): Home, System, Agents, Tasks, Apps. **Modal**: the confirm dialog, the display confirmation, the update popup. Mirrored on purpose, with the reason (the roadmap's "documented reason"): Do Not Disturb — home Data & Storage, mirrored by a Home tile (a quick toggle of a home setting; the bar glyph only shows it). Night light — home Appearance (on/off and temperature), Home tile on/off only. Lid — home Closing the Lid (auto/on/off), Home tile on/auto. Wi-Fi and Bluetooth radios — the panes and the Home tiles (one radio, two quick surfaces). Brightness — the Home slider, QuickControl and keybinds (a slider and its keys; not an advanced setting, so no Management home is correct). Wallpaper and scheme — home Appearance, mirrored by the wallpaper picker and the context menu's "Wallpaper…". Recording target and audio — home Data & Storage, mirrored by the strip at the point of use (both save on change now). Always Unrestricted — one control (Agents page); the Agents tab's chip is an indicator that links to it. Gaming Mode — policy on the Gaming page, the power menu enters it. Blueprint rows vs the live pages — declared state vs live state, by design (BlueprintPage's header). Gaps, recorded, not built: Do Not Disturb's home is Data & Storage because there is no notifications page; Wi-Fi's and Bluetooth's advanced configuration is nmtui / blueman (no network page in Nexus); VPN and Hotspot have no advanced link; the System tab's power tier and Gaming Mode's pinned tier overlap (inferred, unconfirmed). | 13 | — | — |
| 20 Hyprland motion | IMPLEMENTED (apex-os `feat/hypr-motion` 256f55ccd + e74ea99fb fluid retune, not merged; LIVE on the L16's user config since 2026-09-26) | appearance.lua: one motion class per change on the shell's own curves — windows in 240 ms emphasizedDecel / out 175 ms emphasizedAccel (popin 87 %), moves direct (standard 200 ms), fades on effects, focus/dim/border on the state beat, app menus 100 ms, workspaces slide 200 ms, other programs' layers on the small-surface beats; a `no_anim` layer rule for `^quickshell$` | 1 | measured in a nested Hyprland 0.56.2 (session scratch `hypr/probe.sh`): on the stock tree 66–90 % of the network panel's footprint was a translucent blend from 240–345 ms into its open (fadeLayersIn over the pour), ≤ 7 % with the rule; `tests/test-apex-hypr-motion.sh` 15/0 (static half in CI, `--verify-config` half with mutants) | not seen: windows and workspaces in motion — the probe cannot map an app window on the nested output. The shell's motion speed / Reduce Motion do not reach Hyprland yet (Phase 21) |
| 21 Accessibility | IMPLEMENTED (21a–21l) | 21l (after an Opus review of 21j–21k): the drag targets the hit-target check cannot see — raw MouseAreas — were the smallest in the shell: the Home brightness bar grabbed on its 5 px track (32 px now, over its otherwise empty row), the seek bar on 20 px (29: between the player's buttons and its chooser), ChannelColumn's track on 22 px wide (32; the value is read off y alone); the check's header says so. The Agents list had never run with a row in it: the Agent Center smoke (skipped in every sweep for want of an apex-os checkout) now runs against a real apex-agentd with six sessions and a request, and drives it — Tab ×5 to the list, Down onto the first session that needs you, Tab to that row's Pause, Space — and the RUNTIME reports exactly that session paused (mutation: the list no Tab stop pauses nothing). QuickControl's brightness and external-display columns offered a mute button that did nothing, announced as a checkable "Mute Brightness": `muteable: false` shows the icon as a label (no Tab stop, not announced, not dimmed). CfgSegmented forgets its arrow position when its options change (a destroyed pill never reports losing focus). The check counts a tray menu as a popup, not the bar. 21k: CfgSegmented is a radio group — one Tab stop, landing on the chosen pill; the arrows (Left/Right in reading order, Up/Down, Home/End) move between pills WITHOUT choosing, Space/Return choose (many pages apply at once — Light or dark and the colour scheme re-run matugen — so arrowing across options must not apply each); every pill was its own Tab stop. The display-change confirmation's two buttons are real buttons (Tab reaches them, Return presses the focused one; with neither focused Return still keeps and Escape still puts back — Escape now also on the card, an ancestor of the buttons, since the catcher is a sibling). Phase 21's list is covered: Reduce Motion as its own mode, Tab order, arrows in lists and tabs, Enter/Space, Escape, focus restoration, static cues, contrast, hit targets. 21j: hit targets, contrast, the audio pane, the Agents and Config tabs. `tests/check-hit-targets.sh` measures every ApexPressable/ApexIconButton with literal sizes against the design system's own tokens — hitMin 32 px outside the bar, hitBar 24 px (WCAG 2.2 AA) the floor anywhere — counting hitMargin: six Kanban date-picker/card controls were under 24 (the time ▲▼ 20 px: hitMargin 7 now, the digit box between them takes no clicks; the card's ← → ✕ 12 px apart instead of 4 so each takes a 32 px target), three remain under 32, each bounded by a neighbour's glyph (a margin may meet another margin or empty space, never another control). Contrast: the focus ring is accentText on every surface of every shipped palette, 4.21:1 worst (WCAG 1.4.11 wants 3), and the selected fill's step 1.25 (a selection also turns its label to the accent) — both now pinned in color-roles-test; CfgSlider drew the one different ring (1 px of the raw accent at 60 %), now the standard 2 px accentText. The audio pane (AudioControl, in the right panel) carried a pointer-only copy of ChannelColumn: it uses the shared one now (sizes and the worded mute button as settings — captures identical to HEAD), and its device sections are lists (DeviceList.qml: one Tab stop, Up/Down, Return/Space choose the default, focus lands on the current one). Agents tab (Sonnet agent, reviewed): the sessions and requests are one list over all rows in visual order (Up/Down, Return focuses a session's terminal or reviews a request), a highlighted row's buttons join the Tab order, the section heading's action, the guide's entry and the onboarding card's buttons are real buttons, the guide's sections a tab list; corrected after review — the guide and card refocused the entry on every close (a mouse close lit a ring, and a Dashboard close undid its own focus reset): only a keyboard close does now; found live — the guide did not own the keys while up (Tab walked the tab bar and help strip behind it before reaching Close): it opens on its section list and Tab stays inside (KeyNavigation on its two stops — Qt moves Tab focus at the focused item, so an ancestor's Keys never sees Tab). SmallIconButton 26 → 32 px (hitMargin 3; the session row's controls 6 px apart instead of 2). Config tab (Sonnet agent, reviewed): the keybinds list is one Tab stop over every action (Up/Down, Return opens capture), a row's Clear/Reset/pill real buttons while it is highlighted (32 px); it found that a capture opened from the keyboard bound itself to Return (the opening key's release reached the capture) — guarded; the Appearance wallpaper strip is one stop (Left/Right, Return applies); Open in window a real button. Found live — the settings page never scrolled to a control Tabbed to below the fold (the ring sat off-screen): CfgScroll keeps the focused control in view, for every settings page. Still open: CfgSegmented's options are one Tab stop each (a segmented control should be one stop with arrows) — shared by every settings page, its own pass. 21i: the Dashboard's Home and Tasks tabs on the keyboard. Home: Tab follows reading order — left column, centre, right (QuickSettings was declared before the centre column, so Tab went left → right → centre; declaration moved, anchors unchanged); the calendar's ‹ ›, the clock's 11 buttons and its HH:MM (TimeInput: two spin boxes, Up/Down, Home/End; the arrows stay pointer targets; its blue-tinted near-white digits — invisible on a light palette, and allowlisted as a literal — are the palette's text now, EXPECT_FRAC 2 → 0), the player's ⏮ ⏯ ⏭, its chooser rows and every quick-settings tile are real buttons (a checked state on the toggles, names that stay put — Qt 6.10 QML has no Accessible.expanded, measured, so disclosures are checkable too); the player's buttons are disabled for what the player cannot do and all three with no player (they were three dead Tab stops); the seek bar is a slider (Left/Right 5 s, Page 30 s, Home the start) with a 20 px target on a 6 px bar; the brightness bar a slider like the audio ones (5 %, 20 %, Home/End; the service's floor keeps Home off black); a tile Tabbed to below the fold scrolls into view, and the tile grid's clip leaves room for the ring (it was cut on the top row). Tasks: each column's cards one Tab stop (Up/Down, Return opens a card's details, Ctrl+Left/Right moves it a column and keeps it highlighted, Delete asks); a highlighted card's controls join the Tab order; the due-date picker opened from the keyboard lands on its day grid, which is one stop (arrows a day/a week across months, Page a month, Home/End, Return is Done; before a day is chosen the ring sits on today and nothing is selected, so Tabbing past cannot pick a date), Tab wraps inside, Escape and Done hand focus back to Due. It found a pre-existing bug for the pointer too: every edit replaces the task array and the Repeaters rebuild every card, so a card's own `showExtra` reset — picking an urgency closed the panel the chips sit in. Open cards are now a set of ids on the board, and a keyboard edit puts focus back on the rebuilt card's twin (scheduled BEFORE the patch: the rebuild is synchronous, and the chip's handler loses its context mid-way — `root is not defined` on the line after `_patchTask`). The harness itself: headless.sh started every shell on the DESKTOP's session bus (a private XDG_RUNTIME_DIR does not move DBUS_SESSION_BUS_ADDRESS) — the shell under test saw the user's own media players, and a Space on ⏯ would have paused their music; headless_begin now starts a private dbus-daemon (or points at nothing), and ddcutil is stubbed (with no backlight a brightness key drives an external monitor). Kanban converted by a Sonnet agent, Home cards by another, both reviewed and corrected. Found once the clock's own test ran for real (its first version passed while doing nothing — frames showed the Dashboard had closed): (a) the Dashboard window only hides on close, so its focused item survived into the next open and Tab started wherever the last visit left it — a tile, from which the test's keys walked to Screen Capture and closed everything; focus now goes back to the top on close, so every open starts there (reset on the way out, so nothing on open contends with it); (b) the clock's mode tabs were declared after their panels, so Tab went tabs → player and never into the chosen mode; a tab list is now followed by its panel; (c) the alarm add panel collapsed to height 0 but stayed visible, so its spin boxes and Set were invisible Tab stops after closing. 21h: the quick controls' levels are sliders on the keyboard — a Tab stop each, Up/Right and Down/Left 5 %, Page Up/Down 20 %, Home/End the ends, a ring while focused, nothing for a column with no device; the mute button a real button; opened by its keybind the volume slider takes focus at once, and Escape sits on an ancestor of the sliders. ChannelColumn moved out of QuickControl.qml into its own file so it can be driven by the suite. 21g: the notification centre and the clipboard history on the template — each list one Tab stop; notifications: Delete dismisses the highlighted card (the highlight moves on), Return invokes its default action, Clear all / action buttons / ✕ are real buttons, drag-to-dismiss untouched (the seven stack invariants re-asserted); clipboard: Return copies and pastes back (the double-tap), Space or C copies, Delete removes, P pins, Clear and each row's buttons real, and a row's hover-only buttons show while it is keyboard-highlighted (at opacity 0 they were invisible Tab stops). The clipboard popup was still OnDemand and took no key at all (missed in 21b): Exclusive while open, with Escape. 21f: the VPN, Bluetooth and hotspot panes on the Wi-Fi template — each list one Tab stop with Up/Down and Return (the VPN rows are whole-row targets, so they join the list rather than becoming stops; Bluetooth's paired and available lists walk as one), every pointer-only button a real button (11 in Bluetooth, the kill switch and refresh in VPN, show-password and Save in hotspot), the hotspot fields Tab stops. Bluetooth and VPN converted by two Sonnet agents from the finished Wi-Fi diff, reviewed and driven live. 21e: the Wi-Fi pane on the keyboard, the template for the network panes — the network list is ONE Tab stop (Up/Down over connected-then-available, Return = the row's own action), a row's buttons (Disconnect, Forget, Connect, the Forget question, show-password) are real buttons and Tab stops only while their row is highlighted or its panel is open, the header buttons and Wi-Fi-off's Turn On are real buttons, the password fields are Tab stops, Escape closes a row's panel before the pane, and a row action hands the keys back to the list (a focused button that vanishes stranded them). It found a pre-existing bug: the connected row's placeholder (ssid "") read as EXPANDED because "" === _expandSsid's resting value, and on every scan and every open pulled focus into its hidden password field. 21d: Reduce Motion tested as its own mode — every surface captured with it on, sampled at ~50 ms, and read frame by frame (`tests/visual/reduce-motion-report.py`): nothing grows, slides or travels; each surface fades in ~150 ms and a close holds the finished shape while it fades. It found the clipboard and wallpaper sheets popping in and out with no fade at all (still on the legacy duration, 0 under Reduce Motion): both are on SurfaceLifecycle now (legacy durations 12 → 5, named easings 8 → 3). The right panel's Escape moved onto an ancestor of its panes (a focused child made the sibling catcher deaf, as on ArchMenu). 21c: an application's tray menu walks on Up/Down past separators and disabled rows, Return/Enter/Space choose, Right into a submenu, Left/Backspace out; one highlight for the pointer and the arrows, reset on every open. 21b: every transient surface holds the keyboard while it is open — the context menu, ArchMenu, the confirm dialog, and the right panel's network/audio/notification panes (never a toast) are Exclusive while up and None after; QuickControl only when its keybind opened it (hover takes nothing). They were OnDemand or None, and OnDemand is granted only on a click on labwc: measured, Escape and the arrows reached nothing and the menu stayed open. Escape on each, on an ancestor of whatever takes focus. The shared TabSwitcher (Dashboard, network, audio, settings) and the Nexus page list are tab lists — one Tab stop, arrows along the axis, Home/End, a mirrored row reversed, a ring only for keyboard focus, `surfaceSelected` for the pill. The context menu and the power menu walk on Up/Down; the confirm dialog has two real buttons, the confirm focused (so Return still confirms) and its focus shown; the Nexus close button is a real button. 21a: the compositor follows the shell's Motion — speed × Hyprland's own declared values (read back with `hyprctl -j animations`, so apex-os stays the one place they are written), Reduce Motion switches the spatial classes off and caps the fades at 150 ms, scale 0 turns animations off; a per-instance record in $XDG_RUNTIME_DIR stops a shell restart scaling its own push again; a `hyprctl reload` is followed by a re-push. New `motion` capability (Hyprland only). A keyboard audit of every surface (session notes) drives 21b onwards | 3 | 21l: `run-keyboard-nav-test.sh` 17/0 (an unmutable column has no mute stop); `run-agent-center-smoke.sh` against apex-agentd (APEX_OS_ROOT=wt-hypr-motion-os): 6 sessions, 1 request, every state, the keyboard pause read back from the runtime; quick-controls capture identical to 4b5f2c8d (no pixel off by more than 8); full sweep on this tree — see the verification log. 21k: `run-a11y-controls-test.sh` 27/0 (exactly one pill a Tab stop, the chosen one; arrows walk without choosing, Space chooses, re-entry lands on the chosen pill — mutation-tested: every pill a stop fails two); `run-display-transaction-test.sh` 43/0 with a keyboard cycle (Tab to "Put it back now", Return, the output comes back — the old dialog fails it: Return fell to the catcher, which keeps); rtl 37/0, settings-controls, settings-pages, settings-staged, config-agents-keys 7/0, colour-page, input-settings, display-unplug, popup-smoke green. 21j: `tests/check-hit-targets.sh` 3/0 (self-test: comments, strings, nested children, bindings), ratchet 2; `color-roles-test` 18/0 with focusRing ≥ 3 (worst 4.21) and selectedStep ≥ 1.2 (worst 1.25); `run-keyboard-nav-test.sh` 16/0 (DeviceList added); audio pane captures (output, input, mixer) against HEAD: no pixel differs by more than 8 levels; `run-config-agents-keys-test.sh` 7/0 — the guide opens from its entry, takes the section keys, keeps Tab inside, closes on Escape alone (the page back within Δ 1.4) and the keys return to the entry; a wallpaper applied from the strip (a logging awww/swww, ~/.curr_wall) — mutation-tested four ways (no Tab trap, no focus on open, no keyboard-close signal, a strip deaf to Right); settings-pages, settings-staged, popup-smoke, rtl, a11y-controls, colour-page, input-settings, lid-page, privacy-page, dashboard-keys all green, nav-geometry still exactly 16. 21i: `run-keyboard-nav-test.sh` 15/0 (TimeInput added, mutation-tested); `run-dashboard-keys-test.sh` 20/0, and 20/0 under Reduce Motion — the clock section reads the card off frames (the clock keeps no file): an alarm row appears (row band Δ 10.6), its digits dim when switched off (67 → 51), and the list returns to empty on delete (Δ 0.1), Set Timer hands focus back to + (Space reopens its panel, Δ 11.6), a launcher opened after a close still has the first key, the picker's Tab wraps both ways, no script error through the edits — against `tests/lib/fake-mpris.py` (a paused track on the private bus), `fake-brightnessctl` and fake-nmcli: Return on ⏯ → Play, the seek bar → SetPosition +5 s and 0, the slider → set 55 / 100 / 2, the Wi-Fi tile → radio wifi off; three seeded cards (the first id 0) moved twice, deleted, given urgency high and a due date through the day grid, read back from tasks.json; nothing else asked of the player, backlight, NetworkManager or BlueZ. Mutation-tested: the old declaration order (0/5, and Space landed on Bluetooth — `power off` caught by the fake), player buttons live with no player (Tab counts shift), the slider deaf to Up; before the `_expanded` fix the picker step failed exactly as on the pointer (frames: the card collapsed, focus fell to the tab bar). Full headless sweep with the private bus — see the verification log. 21h: `run-keyboard-nav-test.sh` 14/0 (three new: the level's steps and ends, a dead column ignoring keys, Tab to mute and Space); the quick-controls capture differs from before only by the keyboard ring on the volume track (the IPC open is the keybind path); `check-wheel-value` re-pointed at ChannelColumn.qml. 21g: `run-lists-keys-test.sh` 5/0 on a private bus — NotificationClosed(id, 2) and ActionInvoked(id, 'default') read off the bus with gdbus monitor, a history entry deleted and the next copied back against `tests/lib/fake-cliphist` + `fake-wl-copy`, nothing else asked (no wipe); `check-notification-stack` 15/0. 21f: `run-netpanes-keys-test.sh` 6/0 against fake-nmcli + `tests/lib/fake-bluetoothctl` — a tunnel dropped and raised, the connected mouse disconnected and the headphones connected, the hotspot saved from the keyboard, and nothing else asked of NetworkManager or BlueZ (the VPN tab's own load-time autoconnect-off is allowlisted by name). 21e: `run-wifi-keys-test.sh` 5/0 against `tests/lib/fake-nmcli` (canned networks, changes logged never run): Return connects, the row's Disconnect and the Forget question by Tab, nothing else asked of NetworkManager, Escape closes; mutation-tested — with the placeholder bug back the same keys connected to Office, a network nobody chose. 21d: reduce-motion-report over all surfaces: MOVED none except the designed ones (the right panel drops its in-bar band at once on a reduced close, Δ = the notch height; a full-screen scrim under the diff threshold); clipboard/wallpaper re-captured both ways (fade under Reduce Motion, the morph without), cold opens settle. 21c: `run-traymenu-keys-test.sh` 6/0 — the real TrayMenu on a fake StatusNotifierItem + DBusMenu (private bus), the application told the right row, mutation-tested; keys are NOT typed there (a grabbing popup opened without a click serial is dismissed at once, measured 62–135 ms, and the harness has no pointer), so the handler→model mapping is a static check. 21b: `run-keyboard-nav-test.sh` 11/0 (mutation-tested: no Tab stop, no arrow step; a keyboard-moved selection kept in view in a scrolling column, and only then — keeping it on open scrolled the live settings column and nav-geometry caught it); live under wtype in the headless compositor (`capture-surfaces.sh keys`): menu Down/End/Escape, Dashboard Tab→Right, Nexus Tab→Down→Escape, power Tab→Down→Escape, confirm opened from the keyboard with its ring, Left to Cancel, Escape; centre and quick controls close on Escape. 21a: `hypr-motion-test.js` 19/0; `run-hypr-motion-sync-test.sh` 6/0 live in a nested Hyprland, restart assertion mutation-tested (compounds to 15 without the record); compositor facade 60/0; backends 47/0 | Done. Left pointer-only by design: the bar (no keyboard focus; keybinds reach its surfaces), drag handles, scrims. UpdatePopup is dormant on APEX (first-run seeds autoUpdate false). A live test that could touch the machine uses a fake: nmcli, bluetoothctl, cliphist/wl-copy, brightnessctl, an MPRIS player — the harness bus is private now, the tools are not. A live keyboard test of the network panes must use tests/lib/fake-nmcli (and a fake bluetoothctl): the harness reads the REAL NetworkManager and BlueZ, and Return on a row would disconnect the machine. The first wtype key of a session is lost (harness: a modifier tap goes first). UpdatePopup has no keyboard path but is dormant on APEX (first-run seeds autoUpdate false) |
| 22 Frame pacing | IMPLEMENTED (22a; one stall recorded, not fixed) | 22a, measured rather than assumed: `tests/visual/frame-pacing.sh` (the shell in the private labwc on the GPU, gles2) opens and closes every surface cold once and warm five times; with APEX_PACING_LOG=1 each SurfaceLifecycle logs the frames its animation clock advanced, the longest gap, where in the phase it fell and the wait for the first frame, and a /proc watcher attributes every process the shell starts to the open or close it began under. **Fixed:** surfaces forked as they opened — the Dashboard ~8 processes per open (its pages' services went live when the page was shown), the network tabs' nmcli / bluetoothctl and list rebuilds as the pour began (the network pane's first ~130 ms went missing, Fable's review) — now the Dashboard's pages go live when its lifecycle reaches Open and the network tabs refresh on a `settled` flag the right panel hands down: warm opens start no process under their animation, the right panel's worst gap halved (145 → 76 ms). Visible cost, stated: a page shows its previous data for the ~300 ms of a first open before refreshing. **Recorded, not fixed — a ~98 ms pause in the animation clock at ~40 % of every Dashboard open**, the open running 328 ms against 240 designed (the right panel and the power menu show the same shape at ~50 ms; the standalone surfaces — the Nexus sheet 21 ms, the context menu 17 ms — do not). What it is not, by experiment: forks (gone, pause unchanged), the screen border windows (borderWidth 0, unchanged), Qt's frame-callback timeout (20 ms, unchanged), the bar's content (barEnabled false, unchanged). What it is associated with: the render-thread log shows the Dashboard's window delivering 22 frames with a worst gap of 30 ms while the GUI animation clock pauses — the shape freezes on delivered frames — and Quickshell builds a NEW QQuickWindow (a new render thread) every time a layer surface maps. Experiments: Qt's basic render loop halves it (worst 50 ms, opens on their designed time) but moves every window's rendering onto the GUI thread and is measured only headless — recorded, NOT adopted; the wall-clock animation driver (QSG_USE_SIMPLE_ANIMATION_DRIVER) delivers FEWER frames (13–14 per open, gaps to 70 ms) because every window then repaints — rejected. Next step is a measurement on the real panel (APEX_PACING_LOG=1 on the L16, which means opening surfaces on Andre's desktop — not done unasked). Headless cannot measure compositor presentation, mixed refresh or input-to-visible latency; the harness header says so.  | 8–14 | bench, `QSG_RENDER_TIMING` | GPU-side unmeasured |
| 23 Shape QA | IMPLEMENTED (23a, 23b; F1 extends it) | 23a (27c3442): three of the roadmap's rejects asserted over the dense sweep — a parameter that JUMPS (largest step at N=2000 over a quarter of N=200's and over 3 px), a parameter past its endpoints (one named exemption: the pour's designed corner bulge), and every fluid popup's input mask bound to the same geometry result it draws; each with a self-test that must fail. 23b (a29c1de, Fable's slow-motion review): the launcher kept re-flowing to the Home width through its close (Binding restore → `RestoreNone`) and pages dropped live at the close's start (pageLive is a latch). F1 adds the liquid-channel sweeps: every family, including the two rises, along replayed spring trajectories (open, close, both reversals) at 120 Hz. | 5 | `fluid-geometry-test` 386/0 | silhouette sign-off at scale is visual: the fluid-harness sheets (0.85/1/1.5) and the nested-Hyprland captures are the record |
| 24 Stress matrix | IMPLEMENTED (headless half) | `tests/visual/stress-matrix.sh` (QA report, the shell in the private labwc with APEX_PACING_LOG=1, which now also logs every lifecycle map/unmap and the lock's engagement): **spam** — 8 surfaces × 24 toggles 0–150 ms apart, each must end unmapped; **tab switch** twice mid-bloom; **focus mode** flipped under an open Dashboard; **notifications** — 12 in a burst on the private bus, then closed by their sender; **lock** with the Dashboard open must engage; no QML error in the run. **Found and fixed:** a notification its sender closed while it waited in the toast queue still toasted later — the closed burst went on toasting one by one for a minute (the queue now drops anything no longer tracked). 7/7 after the fix. Covered elsewhere: immediate launcher typing (run-dashboard-keys-test), reversal at arbitrary progress (run-surface-lifecycle-test), monitor unplug (run-display-unplug-test), scales (run-nav-geometry-test). | all | stress-matrix 7/0 | mixed refresh, held volume keys (no PipeWire headless), fullscreen games: hardware only |
| F Fluid motion (Andre, 2026-09-26) | IMPLEMENTED (F1–F7) | F7, the review's one open item closed: katana's panel is 144 Hz (EDID 144.03 Hz; the L16's is 60), and Qt's SpringAnimation steps at a fixed 16 ms, so MotionSpring values moved at ~62 Hz there. `SpringFollower` (theme/anim, registered in src/qmldir and the stage) steps the same closed form the surfaces use once per frame on the wall clock: born at its target, `live: false` jumps, Reduce Motion jumps. All 21 MotionSpring sites moved to it — tab pills, the Nexus highlight, switch knobs, the open pill, the workspace dot, the Dashboard / power-menu / right-pane widths, the notch widths, the notch carousel (driven from `onValueChanged`: a ListView overwrites a bound contentY), the percent pills, the disk bar, the audio and brightness fills — and MotionSpring and its exact Qt mapping were deleted as dead code. `run-spring-follower-test` 9/0 (new, in CI). F6, an adversarial review of F1–F4 (Opus, read-only; every finding re-checked here): **a lock asked for during the ~240 ms release window was LOST** — `locked` was still true, so `locked = true` changed nothing, and the release timer then unlocked anyway: a lid closed within a beat of Enter suspended and resumed unlocked. Every lock now goes through `LockState.lock()`, which cancels a pending release, and the release returns unless it is still `unlocking`; `check-lock-writes.sh` (new, in CI) pins who may write `locked` with a mutant per rule, and `locked-hint-test` locks inside the window (47/0). Also: the curtain comes down if the compositor ends an engaged lock the shell still believes held; a spring snapped from inside its own frame handler kept its velocity (the trail started the next open with a backward push — `test_a_closed_surface_is_exactly_at_rest` fails without the fix); `easing.bezierCurve: Motion.spring` named the spring-ROLE function, not a curve, so the lock arrival and the tooltip settle ran linear (`Motion.springCurve`; check-reduce-motion now rejects any curve Motion does not have, with a mutant); a lock surface shown again arrives again; the curtain shows the lock's own background when one is set, and a relock mid-fade keeps it whole; the RTL suite's window-root pin moved to 18 (UnlockCurtain). The recorded SpringAnimation 16 ms limitation is fixed in F7. F4, measured and hardened after an advisor review: **frame pacing** with the springs (`frame-pacing.sh`, gles2, 4 warm): no surface delivers fewer frames than under the curves — dashboard open 20 frames / worst 98 ms at 0.32 (the Phase 22 stall, unchanged, and now invisible because the body waits at the notch until its first frame), nexus 21/25, power 14/52, context 23/17. The labwc-headless harness also shows a 50–92 ms FIRST frame on several closes (right panel, context, clipboard); under nested Hyprland 0.56.2 the same closes start in 5–23 ms (probe2 pacing logs), so that one is the host, not the shell — recorded, not chased. The unlock curtain's wallpaper decodes asynchronously (its window exists from startup, so the pixmap is warm for the lock surface's first frame too). `nav-geometry` measures a rowless page as text ONLY when it shows a declared unavailable state (a page-level EmptyState, or a section with `pageUnavailable`) — every page has a header Text, so "any text" would have accepted a page that lost its rows — with a self-test on a real rowed page and a check that the self-test ran (3195/0). The lock entrance was probed with Andre's own settings, wallpaper (a PNG) and palette before the live deploy. **Known, not a regression:** `run-lockscreen-atspi`, `run-lockscreen-atspi-shim` and `run-recovery-atspi-shim` fail identically on the previous commit — they pin Qt/Quickshell publishing NO accessible tree, and upstream now publishes one ("THIS IS AN IMPROVEMENT"); the shims are obsolete and the real read-back assertions are owed. F3, the lock screen, which had no motion at all: the surface is still opaque from its first frame (the security property is untouched) and on it the wallpaper starts SHARP — the desktop's own — then blurs and dims in over the hero beat while the clock drops into place and the field rises a beat after. A correct password plays it back out (`surface.leave`, surfaceExitSmall) and the lock releases on a timer that IS the release — unconditional, so nothing can hold a session locked after a correct password (`locked-hint-test` 45/0: releases in ~240 ms). `UnlockCurtain` (per output, Overlay, no input): the desktop wallpaper mapped behind the lock once the compositor has ACKNOWLEDGED it (`LockState.lockSecure` → `curtainArmed`; a lock that failed to engage must not hide the desktop), which covers the hand-over with exactly what the lock surface had become and fades the windows and bar in from under it. `WallpaperService` with no config now falls back to APEX-OS's default (`/usr/share/backgrounds/apex/default.jpg`) before the fork's bundled BRAIN SHELL art. F2: the Wi-Fi list is swapped once per scan (it emptied as a scan started, so the refresh at Open blanked it); the launcher holds its resting list while the search stack is released (it read "No apps found" while fading out); notch widths and the status items on MotionSpring; the exclusive zone stays timed (each step re-lays-out every window). F1: `theme/spring.js` + `components/Spring.qml` (closed-form damped spring, response/damping fraction, wall-clock dt clamped to 50 ms); `SurfaceLifecycle` rebuilt on springs — `progress` is the body channel; `liquid: true` adds lead/body/trail channels with release thresholds (the body starts once the lead is 12 % out; on the way out the lead — the part touching the bar — leaves once the body is under 35 %), `bodyFlow`/`leadFlow`; "Open" is perceptual (both channels past 98 %), a close unmaps at a visual zero (0.4 %); content starts once the body is 30 % open, not on a ms delay. **The first frame:** a lifecycle given `surface` holds its springs at 0 — where the body IS the notch — until that window has swapped a frame since it mapped (a 250 ms guard for a window that never reports one): the clock used to run during the 50–150 ms map, so the body appeared a third grown ("the middle notch fades away and a big rectangle appears"). `geometry.js` channels (`g.ch = {w, d, n, fw, fd}`): linear ramps with a soft-capped swell past 1 (bloom W 4 px / D 6 px, pour 5/6, spills and rises 4), the bloom's bottom edge bows ±8 px with the depth flow, the pour's front thins by up to 24 px and its corner rounds +14 px with the width flow; without `g.ch` every family draws exactly what it drew (the derived channels ARE the old curves). Two new families replace the last two scaled rectangles: BOTTOM_RISE (wallpaper picker: the strip swells, then the body rises) and CORNER_RISE (clipboard: up the right strip, then pouring left, the front lower). Dashboard: the notch's content window stays open (odd-even hole + a cover on progress 22–60 %) so the bar's label is seen fading instead of being covered by the first frame. Spills fade their ridge both ways now (the first frame IS the ridge). `MotionSpring` (SpringAnimation, exact mapping — measured: Qt steps 16 ms, `v += k·d − c·v`, matched by eigenvalues; deadbeat under Reduce Motion because spring 0 strands the value; REPLACED in F7 by SpringFollower) for everything retargeted mid-flight: tab pills, the Nexus highlight, switch knobs, page-width retargets, slider fills, the workspace indicator, the notch carousel. Motion table retuned (`motion.js` BASE ~1.7x for surfaces, curves ease out of rest — `tests/motion-test.js` asserts no curve covers 20 % in its first 5 %). **Hyprland was fading every shell layer in over 400 ms** (live L16: stock `fade` 4 ds `ease`) — the shell now declares `hl.layer_rule { namespace ^quickshell$, no_anim }` itself at start and after every reload (HyprlandBackend). apex-os `feat/hypr-motion` e74ea99fb retunes windows/workspaces/fades to the same curves (windows 420/280 from 90 %, moves and workspaces 380 on the fitted spring). | 4–8, 20 | `spring-test` 20/0, `motion-test` 147/0, `fluid-geometry-test` 386/0 (new: the four families + two rises along replayed spring trajectories — open, close, both reversals — at 120 Hz: no kinks, no crossing, attached, clip inside, swell inside caps, no frame jumps, exact at rest), `run-surface-lifecycle-test` 21/0 (first-frame gate, liquid sequencing and overshoot), all 33 check-*.sh; nested Hyprland 0.56.2 captures (session scratch `hypr/probe2.sh`): bloom, pour, clipboard, wallpaper all opaque from their first visible frame and growing out of their origin; apex-os `test-apex-hypr-motion.sh` 15/0 incl. `--verify-config` | real-panel feel is Andre's call; the ~80 ms first-frame wait is now invisible (the body waits at the notch) but is still latency |

## Release gates (roadmap v3 §26), assessed 2026-09-26

| Gate | Verdict | Evidence | What is still open |
|---|---|---|---|
| A Foundation | MET | Motion roles in `Motion.qml`/`motion.js` (`motion-test` 147/0), visual tokens in `ThemeSet`/`roles.js` (`color-roles-test`, `check-color-tokens`), shared interaction in `ApexPressable`/`ApexFocusRing` (`run-controls-primitives-test`), Reduce Motion central and linted (`check-reduce-motion` 29/0, ratchets never raised) | — |
| B Fluid engine | MET | Six families (bloom, pour, two spills, two rises) on springs with velocity kept through reversals; no surface guesses its close (SurfaceLifecycle completion + first-frame gate); `fluid-geometry-test` 386/0 incl. replayed spring trajectories; pacing unchanged from Phase 22 under the springs | GPU numbers are headless (gles2 on the L16's GPU, private labwc), not the real panel |
| C Core surfaces | MET | Dashboard CENTER_BLOOM, right panes RIGHT_POUR, power LEFT_SPILL, quick controls EDGE_SPILL, clipboard CORNER_RISE, wallpaper BOTTOM_RISE, launcher lens inside the bloom, Nexus sheet; connected navigation (pills and highlights on MotionSpring, directional pages); immediate input (`run-dashboard-keys-test` 20/0, launcher first key) | — |
| D UI redesign | MET (product calls open) | Phases 15–18: one state language, the page grammar, colour/material roles (`check-*` gates) | Andre's calls, recorded under Deviations: D.5 band title, the four scrim values, notch radius, the clock face |
| E System UX | MET | One settings home (19a/19b), compositor motion on the shell's curves and LIVE (apex-os `feat/hypr-motion` e74ea99fb; the shell also declares its own no_anim layer rule), keyboard and a11y (21a–21l) | the three AT-SPI shims are obsolete (upstream now publishes a tree): their real read-back assertions are owed |
| F Release | NOT YET MET | reversal without jumps (springs; `run-surface-lifecycle-test` 21/0); no stale surfaces or masks (`stress-matrix` 7/0, 23a mask gate); Reduce Motion complete (lint); no functional regression across 47 headless runners | **frame budget on target hardware** — the springs are live on the L16 as of 2026-09-26 but only Andre's own use there measures it; **mixed refresh** — needs two real panels |

## Also shipped on this branch

| Change | Where | Verification |
|---|---|---|
| Password shapes on the lock screen (and the login screen, apex-os `feat/password-shapes`) — one geometric shape per character, chosen by position only, fed the field's length only | `src/components/auth/PasswordShapes.qml`, `Lockscreen.qml`; apex-os `GreetSurface`/`GreetContext`/`apex-greet-wallpaper` | `check-password-shapes.sh` 13/0 (mutants), `run-password-shapes-test.sh` 12/0, `check-lockscreen-a11y.sh` 33/0, apex-os `test-apex-greet-motion.sh` 25/0; captured on the real lock and login screens; visual advisor review folded in |
| Lock screen: a refused password now clears the field (it left the rejected attempt in place) | `Lockscreen.qml` `fail()` | `check-password-shapes.sh` CLEAR rule + mutant |
| Greeter: a refused login's error survives the field being cleared | apex-os `GreetSurface.qml` | `test-apex-greet-motion.sh` §4 + mutant |
| Harness: the 24 runners that do not source `lib/headless.sh` (nav-geometry, settings-pages, rtl, colour-page, …) were on the DESKTOP's session bus — every NotificationServer, tray watcher and MPRIS client they loaded shared Andre's bus. `lib/private-bus.sh` re-execs each under `dbus-run-session` with a no-activation config (no portal, no secrets service → no keyring prompt), the daemon never sees the display, and the bus dies with the run (b357448a) | `tests/lib/private-bus.sh`, 24 `tests/run-*.sh` | `check-headless-runners.sh` fails a runner on neither bus (self-tested both ways); all 23 re-run green, nav-geometry's 16 identical by name |
| Harness: `run-dashboard-keys-test.sh` takes DASH_KEYS_COLORS (another palette) and its "the alarm dims" check is palette-neutral — dimming is toward the row's surface, which on a light palette is BRIGHTER (the old "darker by 15 %" failed there, 196 → 213) | `tests/run-dashboard-keys-test.sh` | 20/20 dark and light |

## Deviations from the roadmaps

| Where | Roadmap says | Implemented | Why |
|---|---|---|---|
| Segmented control | brief §E: one shared pill travelling between segments | the selection cross-fades per pill on the state beat | CfgSegmented is a Flow that wraps (21 call sites); a pill sliding across wrapped rows reads worse, and a shared pill inside a Flow needs a layout hack or a structural change to every caller |
| Colour roles | brief §C.5: textSecondary mix(T, B, .35), textTertiary .55; fallback checks surfaceHigh only; no tertiary fallback | textSecondary .30, textTertiary .50; the fallback checks surfaceHigh AND surfaceSelected and has a tertiary branch | measured on the twelve real palettes: at .35 the default wallpaper's pale accent put secondary text at 4.38:1 on surfaceSelected, and .55 put tertiary at 2.83:1 on a light scheme — 13 fallbacks fired, one on the shipped default itself (design review 2: a fallback that fires on the default is the formula). At .30 / .50 none fires; the rule stays for unshipped palettes |
| Right notch while a panel is open | brief A.2: the bar widens its notch, `rWidth = W(p)` | the bar's notch stays at its natural width; the panel draws the widened band over it | a moving edge split across two layer surfaces is a frame off itself (measured, D3) |
| Toast while a panel is open | — | not shown; a toast on screen is dismissed when Network or the centre opens; opening the centre drops the queue; a toast arriving while the centre is open is not queued | the body belongs to the open panel; the centre already lists every one of them |
| Toast state | a global `Popups.notificationToastOpen` read by every bar | each screen's panel pushes its own toast state to its own bar | one screen's dismiss used to close every bar's notch |
| Audio popup | RIGHT_POUR | Moves to hang under the right notch as a RIGHT_POUR (its trigger lives in the notch); QuickControl, which is edge-triggered, becomes EDGE_SPILL | The design brief found a pour has no source at mid-edge; the user's instructions and both roadmaps name Audio as RIGHT_POUR. Moving it under its own trigger satisfies both. |
| Dashboard height | — | The finished body's bottom is `notchHeight + dashboardHeight` (was `dashboardHeight` from the screen top) | Pages gain the 40 px the bar used to take, which fixes Home clipping its last tile row (brief §F.1); the setting now means "height below the bar". |
| notchRadius default | brief: 15 → 12 | kept at 15; the bottom corner is now a derived token (14) | A default change is a product call for Andre. |
| Dashboard clock face | Fable (Phase 17): one line `13:53` at typeDisplayMono (56/500 mono — the ladder's "dashboard clock" role, never used) with seconds on its baseline, clockH 220 → 168 | kept: hours stacked over minutes with the diagonal offset, 72 px bold | A signature element, not a layout defect — a product call for Andre. The same for the profile card's height (160 for ~80 of content) and the calendar's stretched rows: resizing them unbalances the left column, and only the look can decide it. |
| Right panel title in the bar's notch | brief §D.5 (optional) | not done; the band's left stays empty | Product call; the brief's own fallback. Fable (Phase 17 critique) recommends YES: the pane title at typeHeading in the empty band beside the open pill, the pane's title row becoming a right-aligned actions row — not implemented, Andre's call. |
| Launcher page | lazy: built on first visit (the performance baseline) | built in the background, asynchronously, once the Dashboard has finished its first open (`LazyPage.prewarm`) | the first Home → Apps switch spent ~390 ms building the page before anything moved (measured per frame); with the prewarm the switch starts by ~150 ms. The cost is one page kept built after the first Dashboard open, on the page the Dashboard exists to host; every other page stays lazy |
| Phase 0 | "Record videos … frame timing at 60 Hz and one high-refresh target" | Timestamped frame bursts at 3.75x slow motion + CPU/frame bench; no high-refresh capture | Headless outputs are 60 Hz; no high-refresh hardware reachable without opening windows on the user's desk. |

## Resume notes (kept current)

**2026-09-26 — SUPER+C now opens Settings (Nexus); the Dashboard's Config tab is gone (Phase 19a, one revertable commit).** The roadmap's own rule — the Dashboard is operational, not a second settings app. `dashboard-config` stays as a compatibility name that opens Nexus. Next in 19: Nexus nav grouping (the Phase 13 deferral), then 22–24.

**2026-09-26 — Phase 21 closed (21l).** Full sweep green bar the known four. Next: Phase 17 (page-level redesign, from 17a) and 18 (colour/material, from 18a); Andre's open product calls are listed under Deviations.

**2026-09-26 — 21i closed after the restart.** The headless sweep covered all 50 runners (51 files; `run-nested-labwc` is a helper), but not on one tip: the first 33 ran against `b52a7694` before the restart, the last 18 against the tree that became `1396a8e6`, and the Dashboard-touching ones (launcher-hover, nav-geometry, popup-smoke, keyboard-nav, rtl) again against the follow-up. Every result is green except the known pre-existing failures (lockscreen-atspi-shim 4, lockscreen-atspi 1, recovery-atspi-shim 2, nav-geometry exactly 16) — the first sweep on headless.sh's private session bus. run-rtl-test caught two new numeric `x:` insets (tile grid, Kanban column): anchors now. The Clock section's first version passed while inspecting nothing; it reads frames now and found three defects (see 21i). Next: Opus scrutiny of 21i, then the rest of Phase 21 (hit targets < 32 px; the Agents and Config tabs).

- Committed through 18a and the rest of visual review 2; Phase 20 is on apex-os
  `feat/hypr-motion` (pushed, no PR). Next: Phase 21 (accessibility: Reduce
  Motion as its own mode, including pushing it and the motion speed to Hyprland
  over `hyprctl eval`), then the rest of 17 (band title, Home tiles) and 18b.
- Nested Hyprland: it starts with NO output — `hyprctl output create headless`
  first, `-j` requests, `LIBSEAT_BACKEND=seatd` so its DRM probe never asks
  logind for the live session (memory: nested-hyprland-needs-output-create).
- Capture a light palette: `CAPTURE_PALETTE=<colors.json>` on capture-surfaces,
  capture-lockscreen and osd-harness; the light default-wallpaper palette is in
  tests/fixtures/palettes-matugen-4.2.0.json.
- The toast capture needs a notification and must never send one to the desk:
  `env -u WAYLAND_DISPLAY -u DISPLAY dbus-run-session -- env APEX_CAPTURE_BUS=private
  tests/visual/capture-surfaces.sh OUT toast` (gdbus Notify — this host's
  notify-send does not deliver to the private bus).
- Before each phase commit: EVERY `tests/check-*.sh` (Phase 7 broke two that a
  narrower list missed: agent-center's lazy-page grep and the white-foreground
  ratchet), `tests/check-reduce-motion.sh`,
  `check-wheel-value.sh`, `node tests/{motion,fluid-geometry}-test.js`, the
  qmltestrunner suites (settings-controls, a11y-controls, rtl, lifecycle,
  password-shapes), `run-nav-geometry-test.sh` (16 pre-existing),
  `run-popup-smoke.sh`, `run-nexus-smoke.sh`, then a capture sheet of the surface.
- Staged quickshell harnesses import `./src/services` before `./src`.
- Fable design brief: session scratchpad `fable-brief-1.md` (shape families, tokens,
  top bar, controls).

## Failures that are NOT this branch's (measured on untouched main)

- QuickControl could not be opened at all since the lazy-popup change
  (`cea90b5`, #2): built only by `Popups.quickOpen`, which nothing sets. FIXED in
  9b. (ArchMenu and the wallpaper picker also have strip hover triggers, but
  both are disabled by design — `hoverEnabled: false`, `allowHover: false` — so
  they open by click/IPC only and the lazy build does not affect them.)
- `tests/labwc-matrix-test.qml` built `PopupDismiss` without its required
  `topBar` (since `6b39094`), so the per-output half built no instances and
  failed; CI skips the suite (no labwc there), which is why nobody saw it.
  FIXED in 9b.
- `tests/run-nav-geometry-test.sh`: 16 — the Privacy page lays out no rows at some scales (identical on `17eec38`).
- `tests/run-lockscreen-atspi.sh`: 1 pinned assertion — this machine's Qt now publishes 8 AT-SPI nodes; the pin itself says that is an improvement.
- `tests/run-lockscreen-atspi-shim.sh`: 4 and `tests/run-recovery-atspi-shim.sh`: 2 — the CONTROL / IN-PROCESS pins of the same upstream change (the tree is no longer empty); the identical set fails on untouched main (`99c5ab7`).
- `tests/check-agent-help.sh` fails (`enable`) when `APEX_OS_ROOT` names a stale apex-os checkout; passes against the current CLI.
- apex-os `tests/test-apex-greet-sessions.sh`: 2 — a new `apex-safe-graphics` session the suite does not expect.
- ~~`tests/check-colour-page.sh`: flaky, ~1 run in 3, on main as well.~~ FIXED in 21j: `code | grep -q` under pipefail — the first match exits grep, the writer takes SIGPIPE, the match reads as 141 (and the negated helper passed at random on a match); `grep -c` now.
- `tests/run-display-transaction-test.sh`: intermittent in a full sequential sweep — "the harness is holding a wrapper pid; a kill would orphan the shell" (the harness's own process bookkeeping, not the page); 42/0 every time alone (seen 2026-09-26 twice).

## Verification log

- 2026-09-26 — Phase 21 closed (21j–21l): the full sweep on one tree, every runner and check, with APEX_OS_ROOT set so the agent suites ran instead of skipping (the Agent Center smoke drove a real apex-agentd; the remote-agent smoke 18/0): everything green except the known pre-existing failures — lockscreen-atspi-shim 4, lockscreen-atspi 1, recovery-atspi-shim 2, nav-geometry exactly 16. An Opus review of 21j–21k found the drag targets the hit check could not see, the Agents list never run with rows, and a mute button that did nothing; all three fixed in 21l.
- 2026-09-26 — Phase 21i: the Dashboard's Home and Tasks tabs on the keyboard (see the phase row). `run-dashboard-keys-test.sh` 18/0 against fake-mpris, fake-brightnessctl and fake-nmcli; each of today's four fixes mutation-tested against it (the collapsed alarm panel left a Tab stop, the mode tabs after their panels, no focus reset on open, the delete touching its row after the edit — the last reproduced `ReferenceError: addAlarmBtn is not defined` exactly). The sweep's 50 runners (split across three tips, see the resume note) show only the known pre-existing failures. Follow-up after an Opus review: the focus reset moved from open to close (on open it could race the launcher taking its field; a new check types "kqv" into a launcher opened straight after a close and expects Kqvkeytest, not the "qv" match); the picker's Tab wrap is exercised (Done → ‹, and back with Shift+Tab — sent as ISO_Left_Tab, what a keyboard sends; `wtype -M shift -k Tab` is a different event KeyNavigation.backtab never sees); the Set Timer / Set Alarm handlers read their focus before closing their panels — measured NOT a bug: in Qt 6.10 a hidden item keeps its active focus, and the old handlers pass the new Set Timer step too, so it is kept only as the pattern that holds either way; the whole runner also passes under Reduce Motion (20/0), now a CI step of its own. A test that asserted "no script error" passed while the Dashboard had closed under it — the frames said so; it asserts what the card shows now.
- 2026-09-26 — Phase 21a: the compositor on the shell's Motion, measured live (see the phase row). Keyboard audit (read-only agent) of every surface: F1 four windows never receive keys (keyboardFocus None: QuickControl, ArchMenu, right panel audio/notifications); F2 the shared TabSwitcher is pointer-only; menus have no arrows; ConfirmDialog's Return confirmed regardless of focus; exemplary: the lock screen and the wallpaper picker.
- 2026-09-26 — Visual review 2, the rest. The stack's cards built while the centre was closed sat jammed at rest: their height counted lines by `visible`, the EFFECTIVE visibility, false while an ancestor is hidden (now the show conditions; a new SHOWN rule in check-notification-stack). A removal closes up a beat late (`removeDisplaced`, contentDelay) with the leaving card gone on the content beat; an arrival waits the same beat. The panel's depth then had to wait too, or it clipped the rising card — as a Behavior its pause read the old depth (the Behavior started before the binding beside it re-evaluated; logged), so the depth retarget is driven. Nexus: the sheet leaves on the scrim's beat and the scrim follows the sheet's channel on close (was 69 % scrim over 15 % sheet at 187 ms; now within 0.02 every frame); radiusXL, page title 18/600. SysTray on Motion tokens and without a bar hover fill (legacy 14 → 12, easing 10 → 8).
- 2026-09-26 — Phase 20 (apex-os): measured the shell's layer being faded by Hyprland before writing the rule; see the phase row.
- 2026-09-26 — Phase 18a: the translucent-white migration. Three Sonnet agents converted 481 sites on a written rule set (text → a text role by alpha; fill/border → the palette's text at the same alpha; a fixed surface → an explicit fixed colour). Reviewed after: three ternaries whose two states landed on one role, and the calendar's in/out-of-month days, restored; four empty-state glyphs promoted from .08–.20 to tertiary text put back to a decorative outline; the player card's unconditional scrim made it a dark slab in light, so it draws on the palette without art. Captured every surface tests/visual can open in both schemes of the default wallpaper: all legible, no QML errors. Light or dark returned to Appearance.
- 2026-09-26 — Accessibility regression found by the sweep and fixed: ApexIconButton (Phase 3) published its glyph as its accessible name, so every bar button reached AT-SPI as a private-use codepoint (the lock-screen and recovery shims' glyph rule, 5 strings). It takes a `label` now and never the glyph; the bar's nine buttons are named for what they do. The shims now fail exactly main's pins.
- 2026-09-26 — Visual review 2 (Fable, over every family's capture sheet): each surface reads as its intended family. Applied here: the context menu's frame was the strip's 6 px border (now a 1 px outlineSoft hairline) and its rows, like the tray menu's, flooded with the accent on hover (now the state layer, textPrimary label); the open pill and the Nexus nav pill share surfaceSelected; Nexus nav rows 36 px on radiusM, selection by fill and colour only (no bold); bar cluster gaps even (Audio's trailing 6 px, the clock's padding); the text-role mixes moved to .30 / .50, so no shipped palette needs a fallback (was 13, including the default); the launcher page prewarmed (see Deviations). Waiting on the Phase 18 migration's files: the stack's add/remove pause and its rest overlap, Nexus's scrim outliving its sheet, SysTray's legacy animation. Full sweep green on an isolated tree (nav-geometry 16 pre-existing).
- 2026-09-26 — Phases 2-3: roles over twelve palettes (13 fallbacks, pinned); primitives suite 10/0; migrated controls captured in Nexus. Found in passing: NavPane opened scrolled past its current page (reveal before layout, pre-existing) and its new pill was missing on first open — both fixed. Two Qt traps recorded in the code: a function named `layer` is shadowed by Item's `layer` group; emitting a derived signal straight from an inherited signal's handler threw.
- 2026-09-26 — Phase 14: OSD captured at 1x through a new harness (entrance, held key tracking without replay, exit); fixed the bar growing from 0 on first show; context menu captured (keybind open, centred). Sweep green.
- 2026-09-26 — Phase 12: launcher tab switch captured (pill, body 900→560, field at once, results group reveal). A suspected first-frame lag on Dashboard opens was measured (bloom width per frame, with and without a first-frame gate): the bloom is visible from ~120 ms either way — the gate was reverted; the earlier "nothing for 300 ms" read was depth-only measurement plus IPC latency. Full sweep green (nav-geometry 16 pre-existing).
- 2026-09-26 — Visual review of Phases 9-10 (Fable, over the capture sheets): all three families read as intended and distinct; no geometry.js change. Applied: pane cross-fade waits `fadeOut` (was a double exposure); Reduce Motion close drops the band at once and fades only the body; the spills' ridge fade on exit only; toast accent/progress clear of the melt corner; the centre's heading left-aligned; open pill tint .22. Found while applying: a flag close of the quick controls waited out the hover grace (animDuration + 200, 1.4 s at capture scale) — now immediate; and the spills' opacity was a binding loop (reading `life.open` beside `life.alpha` runs the lifecycle's open handler, which writes alpha) — SurfaceLifecycle gains a side-effect-free `closing`. Deferred with reasons: text wipes and the open-start hitch are sub-frame at 1x (verify in Phase 22); the depth hairline is Phase 18; a band title (D.5) is now possible since the panel draws the band — a pane-layout change for Phase 17.
- 2026-09-26 — Phase 11: stack arrive/remove captured; four silent ListView failure modes found by measurement (replaced-array model, destroyed-QObject model value, content-sized view releasing exits, untargeted nested exit animations) plus a polish-time height; all guarded by check-notification-stack.sh. Full sweep green.
- 2026-09-26 — Phase 10: LEFT_SPILL captured open/close (horizontal extrusion, then unfold; fold, then retract); the close no longer ends in a translucent box (fade keyed to width). Full sweep green.
- 2026-09-26 — Phase 9b: audio pane and EDGE_SPILL quick controls captured; full `check-*.sh` sweep, node suites, headless suites green (nav-geometry 16 pre-existing); RTL window-root pin 15 → 16 (QuickControl is a PanelWindow).
- 2026-09-26 — Phase 9a: one right panel; seam measured per frame on cold/warm open and close and both pane switches — exact after moving the band into the panel (was 35-77 px off with a Canvas bar, then one-frame ledges with a Shape bar); toast captured on a private bus; Reduce Motion close fades the finished shape. All `check-*.sh` green, node suites green, headless suites green except nav-geometry's 16 pre-existing. Fixed two Phase 7 regressions found by the full sweep.
- 2026-09-26 — Phase 7: shared tab pill + directional pages on the Dashboard; forward/back tab sheets reviewed (pill lands ~290 ms, no pop); suites as in the phase row.
- 2026-09-26 — Phase 8a: Dashboard on CENTER_BLOOM; cold open no longer squeezes the page; lint 22/0 (legacy 40, easing 35, unresolved 4); popup smoke, nexus smoke, scaling 88/0; nav-geometry 16 (pre-existing).
- 2026-09-25 — password shapes (both screens), tokens, four shape families (geometry 129/0), Phase 1 motion + migration, lifecycle.
- 2026-09-25 — Phase 0: baseline captured from `17eec38`; renderer bench (idle 0.63, shape 0.80, geometry 0.93, canvas 2.28, busy-control 2.37 ms/frame).
